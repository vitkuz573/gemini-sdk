---
phase: 13-core-protocol-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants.rs
  - src/proto/mod.rs
  - src/proto/indices.rs
  - src/session.rs
  - src/client.rs
  - src/lib.rs
  - Cargo.toml
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02
  - MAINT-03
  - MAINT-04
  - MAINT-05

must_haves:
  truths:
    - "All batchexecute URL paths are built from named constants, not inline strings"
    - "All batchexecute query keys (rpcids, source-path, hl, _reqid, rt, bl, f.sid) are named constants"
    - "All batchexecute transport markers (wrb.fr, XSSI prefix, f.req, endpoint discriminator) are named constants"
    - "All WIZ/session extraction keys (S06Grb, oPEP7c, FdrFJe, cfb2h, f.sid) are named constants"
    - "All RPC identifiers have consistent pub(crate) const definitions, including otAQ7b/Fd0Qje"
    - "cargo test --all-targets, cargo clippy --all-targets -- -D warnings, and cargo doc --no-deps pass"
  artifacts:
    - path: "src/constants.rs"
      provides: "Cross-cutting protocol constants: URLs, query keys, transport markers, WIZ keys"
    - path: "src/proto/indices.rs"
      provides: "Existing parser/builder indices; updated to use constants for wrb.fr and XSSI prefix"
    - path: "src/proto/mod.rs"
      provides: "Batchexecute body builders; updated to use RPC-id and transport constants"
    - path: "src/session.rs"
      provides: "Session extraction; updated to use WIZ-key constants"
    - path: "src/client.rs"
      provides: "Client request building; updated to use path/query/header constants"
    - path: "src/lib.rs"
      provides: "Module re-export for constants"
  key_links:
    - from: "src/constants.rs"
      to: "src/client.rs"
      via: "Client methods import URL, query, and path constants"
    - from: "src/constants.rs"
      to: "src/proto/mod.rs"
      via: "Batchexecute body builders import RPC-id and transport constants"
    - from: "src/constants.rs"
      to: "src/session.rs"
      via: "Session extraction imports WIZ-key constants"
---

<objective>
Create a cross-cutting `src/constants.rs` module that centralizes protocol literals (URL paths, batchexecute query keys, transport markers, WIZ/session keys) and complete the RPC-ID constant set. Refactor `src/client.rs`, `src/proto/mod.rs`, `src/session.rs`, and `src/lib.rs` to consume these constants without changing public API behavior.

Purpose: Eliminate the largest source of magic strings in the transport and session layers, making protocol drift safer to detect and update.
Output: New `src/constants.rs`, updated protocol/session/client modules, and green quality gates.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/client.rs
@src/proto/mod.rs
@src/session.rs
@src/proto/indices.rs
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/constants.rs with core protocol constants</name>
  <files>src/constants.rs, src/lib.rs</files>
  <action>
    Create `src/constants.rs` and add `pub mod constants;` in `src/lib.rs` after `pub mod chat;`.

    Define the following public modules and `pub(crate) const` items:

    - `pub mod urls`:
      - `pub(crate) const GEMINI_BASE_URL: &str = "https://gemini.google.com";`
      - `pub(crate) const WAA_BASE_URL: &str = "https://waa-pa.clients6.google.com";`
      - `pub(crate) const OGADS_BASE_URL: &str = "https://ogads-pa.clients6.google.com";`
      - `pub(crate) const PUSH_UPLOAD_BASE_URL: &str = "https://push.clients6.google.com";`
      - `pub(crate) const APP_PATH: &str = "/app";`
      - `pub(crate) const APP_LANGUAGE_PATH_TEMPLATE: &str = "/app?hl={}";` (used with format!)
      - `pub(crate) const BATCHEXECUTE_PATH: &str = "/_/BardChatUi/data/batchexecute";`
      - `pub(crate) const CONVERSATION_ACTION_SOURCE_PATH_TEMPLATE: &str = "/app/{conversation_id}";`
      - `pub(crate) const USAGE_SOURCE_PATH: &str = "/usage";`
      - `pub(crate) const SCHEDULED_SOURCE_PATH: &str = "/scheduled";`
      - `pub(crate) const DEFAULT_SOURCE_PATH: &str = "/";`

    - `pub mod query_keys`:
      - `pub(crate) const RPCIDS: &str = "rpcids";`
      - `pub(crate) const SOURCE_PATH: &str = "source-path";`
      - `pub(crate) const HL: &str = "hl";`
      - `pub(crate) const REQID: &str = "_reqid";`
      - `pub(crate) const RT: &str = "rt";`
      - `pub(crate) const RT_VALUE: &str = "c";`
      - `pub(crate) const BL: &str = "bl";`
      - `pub(crate) const F_SID: &str = "f.sid";`

    - `pub mod transport`:
      - `pub(crate) const RPC_FRAME_MARKER: &str = "wrb.fr";`
      - `pub(crate) const F_REQ_KEY: &str = "f.req";`
      - `pub(crate) const BATCHEXECUTE_ENDPOINT: &str = "batchexecute";`
      - Use the existing `ANTI_XSSI_PREFIX` from `src/proto/mod.rs` by moving it here as `pub const ANTI_XSSI_PREFIX: &str = ")] } ' \n\n";` and re-exporting from `proto/mod.rs`.

    - `pub mod wiz_keys`:
      - `pub(crate) const S06_GRB: &str = "S06Grb";`
      - `pub(crate) const OPEP_7C: &str = "oPEP7c";`
      - `pub(crate) const FDR_FJE: &str = "FdrFJe";`
      - `pub(crate) const CFB2H: &str = "cfb2h";`
      - `pub(crate) const F_SID: &str = "f.sid";`
      - `pub(crate) const SESSION_ID: &str = "session_id";`

    - `pub mod rpc_ids`:
      - Keep existing RPC-id constants in their feature modules (`conversation_actions.rs`, `user_profile.rs`, `locale_model_config.rs`, `settings.rs`).
      - Add missing `pub(crate) const OTAQ7B_RPC_ID: &str = "otAQ7b";` and `pub(crate) const GET_USER_STATUS_RPC_ID: &str = "Fd0Qje";` here because they currently only exist as inline strings in `src/proto/mod.rs` and `src/client.rs`.

    Add concise doc comments for every constant. Do NOT change any public API signatures. Do NOT make constants public unless they are already exposed indirectly.
  </action>
  <verify>
    <automated>cargo check --all-targets</automated>
  </verify>
  <done>
    - `src/constants.rs` exists and compiles.
    - `src/lib.rs` re-exports the module.
    - All declared constants have doc comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor proto and session modules to use constants</name>
  <files>src/proto/mod.rs, src/proto/indices.rs, src/session.rs</files>
  <action>
    In `src/proto/mod.rs`:
    - Move `ANTI_XSSI_PREFIX` to `src/constants.rs` and re-export it as `pub use crate::constants::transport::ANTI_XSSI_PREFIX;`.
    - Replace inline `"otAQ7b"` in `build_batchexecute_body` with `crate::constants::rpc_ids::OTAQ7B_RPC_ID`.
    - Replace inline `"K4WWud"` in `build_k4wwud_body` (if present) with the appropriate constant from `constants::rpc_ids`.
    - Replace `"f.req"` usage with `crate::constants::transport::F_REQ_KEY`.

    In `src/proto/indices.rs`:
    - Replace `pub const RPC_ID: &str = "wrb.fr";` with `pub use crate::constants::transport::RPC_FRAME_MARKER as RPC_ID;` (or update the const to use it).
    - Keep `META_TOKEN_KEY_26` and `META_TOKEN_KEY_21` as numeric string constants are protocol-index keys, but add a doc comment explaining they are WIZ map keys.

    In `src/session.rs`:
    - Replace all literal `"S06Grb"`, `"oPEP7c"`, `"FdrFJe"`, `"cfb2h"`, `"f.sid"`, `"session_id"` references with constants from `crate::constants::wiz_keys`.
    - Update doc comments and error messages that repeat the literal key names to reference the constant docs instead (do not remove the key names from user-facing error strings; keep them via the constant).

    Do NOT rename the existing error variants or change diagnostic behavior.
  </action>
  <verify>
    <automated>cargo test --lib proto --lib session -- --nocapture</automated>
  </verify>
  <done>
    - `proto` and `session` unit tests pass.
    - No inline occurrences of the centralized WIZ keys remain in `src/session.rs` or `src/proto/mod.rs` (verified by `grep`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor client.rs to use path and query constants</name>
  <files>src/client.rs</files>
  <action>
    Update `src/client.rs` to use constants from `crate::constants` for URL construction and batchexecute parameters:

    - Replace `"https://gemini.google.com"`, `"https://waa-pa.clients6.google.com"`, `"https://ogads-pa.clients6.google.com"` with constants from `constants::urls`.
    - Replace `"/_/BardChatUi/data/batchexecute"` with `constants::urls::BATCHEXECUTE_PATH`.
    - Replace `"/app"`, `"/app?hl="`, `"/app/{conversation_id}"`, `"/usage"`, `"/scheduled"`, `"/"` source-path literals with `constants::urls` helpers/templates.
    - Replace query-key string literals `"rpcids"`, `"source-path"`, `"hl"`, `"_reqid"`, `"rt"`, `"bl"`, `"f.sid"` and `"c"` value with `constants::query_keys` items.
    - Replace `"batchexecute"` endpoint discriminator passed to `build_headers` with `constants::transport::BATCHEXECUTE_ENDPOINT`.
    - Replace inline `"otAQ7b"` / `"Fd0Qje"` references with `constants::rpc_ids` items.

    Keep string formatting (`format!`) where the template is now a constant. Do NOT change header constants (those are covered in Phase 15).
  </action>
  <verify>
    <automated>cargo test --lib client -- --nocapture</automated>
  </verify>
  <done>
    - Client unit tests pass.
    - No inline batchexecute path, query-key, or source-path literals remain in `src/client.rs` (verified by `grep`).
  </done>
</task>

<task type="auto">
  <name>Task 4: Quality gates and final verification</name>
  <files>src/constants.rs, src/proto/mod.rs, src/proto/indices.rs, src/session.rs, src/client.rs, src/lib.rs</files>
  <action>
    Run the full quality gate suite and fix any warnings or failures:

    1. `cargo test --all-targets`
    2. `cargo clippy --all-targets -- -D warnings`
    3. `cargo doc --no-deps`

    Address any failures caused by the refactor. Common issues:
    - Missing doc comments on public items (`#![deny(missing_docs)]` is enabled).
    - Unused imports after moving constants.
    - Broken intra-doc links.

    Also verify with targeted greps that the literal strings centralized in this phase no longer appear inline in `src/`:
    - `"/_/BardChatUi/data/batchexecute"`
    - `"rpcids"`, `"source-path"`, `"_reqid"`, `"f.sid"`
    - `"S06Grb"`, `"oPEP7c"`, `"FdrFJe"`, `"cfb2h"`
    - `"wrb.fr"` should only appear in `src/constants.rs` and `src/proto/indices.rs` via the constant.
  </action>
  <verify>
    <automated>cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps</automated>
  </verify>
  <done>
    - `cargo test --all-targets` passes.
    - `cargo clippy --all-targets -- -D warnings` passes.
    - `cargo doc --no-deps` builds with no warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Constants → request construction | Centralized strings still cross the network; no semantic change to trust model. |
| WIZ-key constants → session parser | Same extraction logic, only string source changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure | Constants module | low | accept | Constants are protocol metadata; no secrets are centralized here. Secret headers/cookies remain handled by existing redaction. |
| T-13-02 | Tampering | URL path constants | low | mitigate | Centralization makes accidental drift harder; no runtime behavior change. |
| T-13-SC | Tampering | No new packages installed | — | mitigate | No package-manager install tasks; uses existing crate only. |
</threat_model>

<verification>
- Unit tests in `src/proto/`, `src/session.rs`, and `src/client.rs` verify behavior is unchanged.
- Full suite passes: `cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`.
- Targeted greps confirm centralized literals are no longer inlined in `src/`.
</verification>

<success_criteria>
- `src/constants.rs` exists and is imported by `src/lib.rs`.
- URL paths, batchexecute query keys, transport markers, and WIZ/session keys are centralized.
- All existing RPC identifiers are constants; missing `otAQ7b`/`Fd0Qje` constants are added.
- Quality gates are green.
</success_criteria>

<output>
Create `.planning/phases/13-core-protocol-constants/13-01-SUMMARY.md` when done.
</output>
