---
phase: 15-infrastructure-constants
plan: 01
type: execute
wave: 1
depends_on:
  - 13-01
  - 14-01
files_modified:
  - src/constants.rs
  - src/client.rs
  - src/har.rs
  - src/transient_400.rs
  - src/metrics.rs
  - src/attestation.rs
  - src/tool.rs
  - src/upload.rs
autonomous: true
requirements:
  - MAINT-10
  - MAINT-11
  - MAINT-12
  - MAINT-13
  - MAINT-14
  - MAINT-15
  - MAINT-16

must_haves:
  truths:
    - "Base URL, static header names/values, User-Agent, sec-ch-ua, and fetch-metadata constants are centralized"
    - "HAR/redaction strings (version, creator, MIME types, secret header names, cookie patterns) are centralized"
    - "Transient WIZ 400 markers and NotSignedIn diagnostic strings are centralized"
    - "Tracing span/metric operation names and metric names are centralized"
    - "Browser attestation CDP method names, selectors, and default strings are centralized"
    - "Tool schema keys are centralized"
    - "cargo test --all-targets, cargo clippy --all-targets -- -D warnings, and cargo doc --no-deps pass"
  artifacts:
    - path: "src/constants.rs"
      provides: "headers, har, transient, tracing, attestation, tool modules"
    - path: "src/client.rs"
      provides: "Uses header and base-url constants"
    - path: "src/har.rs"
      provides: "Uses HAR/redaction constants"
    - path: "src/transient_400.rs"
      provides: "Uses transient-marker constants"
    - path: "src/metrics.rs"
      provides: "Uses metric name constants"
    - path: "src/attestation.rs"
      provides: "Uses CDP/attestation constants"
    - path: "src/tool.rs"
      provides: "Uses tool schema key constants"
  key_links:
    - from: "src/constants.rs"
      to: "src/client.rs"
      via: "build_headers uses header constants"
    - from: "src/constants.rs"
      to: "src/har.rs"
      via: "HAR writer uses version/creator/redaction constants"
    - from: "src/constants.rs"
      to: "src/transient_400.rs"
      via: "Transient detector uses marker constants"
    - from: "src/constants.rs"
      to: "src/metrics.rs"
      via: "Tests use metric name constants"
    - from: "src/constants.rs"
      to: "src/attestation.rs"
      via: "CDP methods and selectors use constants"
    - from: "src/constants.rs"
      to: "src/tool.rs"
      via: "tool_declaration uses schema key constants"
---

<objective>
Centralize infrastructure-level strings: static HTTP headers and values, HAR/redaction strings, transient WIZ 400 markers, tracing/metrics operation names, browser attestation CDP strings, and tool schema keys. Extend `src/constants.rs` and refactor `src/client.rs`, `src/har.rs`, `src/transient_400.rs`, `src/metrics.rs`, `src/attestation.rs`, and `src/tool.rs` without changing public API behavior.

Purpose: Remove remaining high-risk magic strings from headers, diagnostics, telemetry, attestation, and tool handling.
Output: Extended constants module, refactored infrastructure modules, and green quality gates.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-protocol-constants/13-01-SUMMARY.md
@.planning/phases/14-model-chat-upload-constants/14-01-SUMMARY.md
@src/client.rs
@src/har.rs
@src/transient_400.rs
@src/metrics.rs
@src/attestation.rs
@src/tool.rs
@src/upload.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/constants.rs with infrastructure constants</name>
  <files>src/constants.rs</files>
  <action>
    Extend `src/constants.rs` with six new modules. All items are `pub(crate) const` with doc comments unless already public.

    - `pub mod headers`:
      - Header names: `CONTENT_TYPE`, `USER_AGENT`, `ORIGIN`, `REFERER`, `X_SAME_DOMAIN`, `CACHE_CONTROL`, `PRAGMA`, `X_CLIENT_DATA`, `COOKIE`, `AUTHORIZATION`, `SET_COOKIE`, `ACCEPT`.
      - Header values: `X_SAME_DOMAIN_VALUE = "1"`, `CACHE_CONTROL_NO_CACHE = "no-cache"`, `PRAGMA_NO_CACHE = "no-cache"`.
      - `sec-ch-ua` full value constant: `SEC_CH_UA = "\"Not A(Brand\";v=\"99\", \"Google Chrome\";v=\"146\", \"Chromium\";v=\"146\"";`
      - `SEC_CH_UA_MOBILE = "?0"`, `SEC_CH_UA_PLATFORM = "\"Windows\"";`
      - `SEC_FETCH_DEST = "empty"`, `SEC_FETCH_MODE = "cors"`, `SEC_FETCH_SITE_SAME_ORIGIN = "same-origin"`, `SEC_FETCH_SITE_CROSS_SITE = "cross-site"`.
      - `X_GOOG_API_KEY`, `X_USER_AGENT` (`grpc-web-javascript/0.1`).
      - WAA/OGADS specific: reuse `X_GOOG_API_KEY`, `X_USER_AGENT`, `COOKIE`, `USER_AGENT`, `REFERER`, `ORIGIN`, `X_CLIENT_DATA`.
      - `X_GOOG_EXT_PREFIX = "x-goog-ext-"`.

    - `pub mod user_agents`:
      - `pub(crate) const BROWSER_LIKE: &str = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36";`
      - `pub(crate) const UPLOAD_BROWSER_LIKE: &str = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/133.0.0.0 Safari/537.36";` (de-duplicate later in this phase).

    - `pub mod har`:
      - `pub(crate) const VERSION: &str = "1.2";`
      - `pub(crate) const CREATOR_NAME: &str = "gemini-sdk";`
      - `pub(crate) const REQUEST_MIME_TYPE: &str = "application/x-www-form-urlencoded;charset=UTF-8";`
      - `pub(crate) const RESPONSE_MIME_TYPE: &str = "application/json";`
      - `pub(crate) const REDACTED_VALUE: &str = "<redacted>";`
      - Secret header names: `COOKIE`, `AUTHORIZATION`, `SET_COOKIE`, plus `X_GOOG_EXT_PREFIX` from headers.
      - Cookie/auth pattern constants from `src/har.rs`: `SECURE_1_PSID`, `SECURE_1_PSIDCC`, `SECURE_3_PSID`, `SECURE_3_PSIDCC`, `SAPISID`, `APISID`, `SSID`, `HSID`, `SID`, `SOCS`, `AUTHORIZATION_BEARER_PREFIX = "authorization=Bearer "`, `ACCESS_TOKEN_PREFIX = "access_token="`.

    - `pub mod transient`:
      - `pub(crate) const ER_MARKER: &str = "\"er\"";`
      - `pub(crate) const DI_MARKER: &str = "\"di\"";`
      - `pub(crate) const HTTPRM_MARKER: &str = "\"af.httprm\"";`
      - `pub(crate) const SIGN_IN_REDIRECT_TITLE_SUBSTRING: &str = "Sign in - Google Accounts";`

    - `pub mod tracing`:
      - `pub(crate) const SPAN_PREFIX: &str = "gemini.";`
      - Operation name constants for each public client method: `REGENERATE_TURN`, `RATE_TURN`, `DELETE_TURN`, `GET_USER_INFO`, `GET_LAST_SELECTED_MODE`, `SET_LAST_SELECTED_MODE`, `GET_LOCALE_TOOLS`, `GET_MODEL_CONFIG`, `GET_LOCALE_CONFIG`, `GET_TOOLS_CONFIG`, `GET_USAGE_STATS`, `GET_SCHEDULED_PROMPTS`, `LIST_MODELS`, `GENERATE`, `GENERATE_WITH_TOOLS`, `GENERATE_STREAM`, `GENERATE_WITH_CONVERSATION`, `UPLOAD_WITH_PROGRESS`, `VERIFY_SIGNED_IN`, `DIAGNOSE_SIGNED_IN`, `WAA_INIT_CHAIN`, `PARSE_RESPONSE`, `GENERATE_RAW`, `INGEST_CONVERSATION_STATE`.
      - Metric name constants: `METRIC_REQUESTS = "gemini_sdk.requests"`, `METRIC_RETRIES = "gemini_sdk.retries"`, `METRIC_REQUEST_LATENCY = "gemini_sdk.request_latency"`.
      - Attribute key constants: `OPERATION`, `STATUS`, `CATEGORY`, `BYTES`.

    - `pub mod attestation`:
      - CDP method names: `RUNTIME_ENABLE`, `NETWORK_ENABLE`, `PAGE_ENABLE`, `NETWORK_SET_COOKIE`, `PAGE_NAVIGATE`, `RUNTIME_EVALUATE`.
      - Default values: `CHROME_DOMAIN = ".google.com"`, `CHROME_PATH = "/"`, `NAVIGATE_URL_TEMPLATE = "https://gemini.google.com/app?hl={}"`, `LANGUAGE_FOR_ATTESTATION = "en"`.
      - Selector: `SEND_BUTTON_SELECTOR = "[data-test-id=\"send-button\"]"`, `SEND_BUTTON_ARIA_LABEL = "button[aria-label*=\"Send\"]"`, `TEXTAREA_SELECTOR = "textarea"`.
      - Chrome flags as a `&[&str]` constant: `CHROME_FLAGS`.
      - Profile path: `CHROME_PROFILE_PATH = "/tmp/gemini-sdk-chrome-profile"`.
      - Timeout constants (if not already present): `NAVIGATE_TIMEOUT_SECONDS = 60`, `CAPTURE_TIMEOUT_SECONDS = 60`.

    - `pub mod tool_schema`:
      - `pub(crate) const TYPE: &str = "type";`
      - `pub(crate) const OBJECT: &str = "object";`
      - `pub(crate) const PROPERTIES: &str = "properties";`
      - `pub(crate) const REQUIRED: &str = "required";`
      - `pub(crate) const NAME: &str = "name";`
      - `pub(crate) const PARAMETERS: &str = "parameters";`

    Do NOT change behavior; only centralize strings.
  </action>
  <verify>
    <automated>cargo check --all-targets</automated>
  </verify>
  <done>
    - `src/constants.rs` compiles with the new modules.
    - All constants have doc comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor headers and base URLs in client.rs and upload.rs</name>
  <files>src/client.rs, src/upload.rs</files>
  <action>
    In `src/client.rs`:
    - Replace the module-level `WEB_BASE_URL`, `WAA_BASE_URL`, `OGADS_BASE_URL` constants with `crate::constants::urls` items.
    - Replace `USER_AGENT` and `X_CLIENT_DATA` with `crate::constants::user_agents::BROWSER_LIKE` and `crate::constants::headers::X_CLIENT_DATA`.
    - In `build_headers`, replace all header name/value literals with `crate::constants::headers` constants.
    - Replace `"application/json+protobuf"`, `"grpc-web-javascript/0.1"`, `"Cookie"`, `"User-Agent"`, `"Referer"`, `"Origin"`, `"x-client-data"` in `waa_create`/`ogads_get_async_data` with constants.
    - Replace `"application/x-www-form-urlencoded;charset=UTF-8"` with the MIME constant.
    - Replace `"GET"`/`"POST"` strings passed to `maybe_record_har` with constants from a new `crate::constants::http_methods` module (`GET`, `POST`).
    - Replace `"/app"` in `fetch_app_page` and `accept_consent_and_refresh` with the URL constant.
    - In `build_not_signed_in_error`, move the long cookie list sentence into `crate::constants::auth::MISSING_LEGACY_COOKIES_ADVICE`.

    In `src/upload.rs`:
    - Replace `USER_AGENT` with `crate::constants::user_agents::UPLOAD_BROWSER_LIKE`.
    - Replace sec-ch-ua and other header value literals with `crate::constants::headers` constants.

    Do NOT change public method signatures.
  </action>
  <verify>
    <automated>cargo test --lib client --lib upload -- --nocapture</automated>
  </verify>
  <done>
    - Client and upload unit tests pass.
    - No inline header name/value literals remain in `src/client.rs` except where formatting requires it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor har, transient_400, metrics, attestation, and tool modules</name>
  <files>src/har.rs, src/transient_400.rs, src/metrics.rs, src/attestation.rs, src/tool.rs</files>
  <action>
    In `src/har.rs`:
    - Replace `"1.2"`, `"gemini-sdk"`, `"application/json"`, `"application/x-www-form-urlencoded;charset=UTF-8"`, `"<redacted>"` with `crate::constants::har` constants.
    - Replace header-name string literals in `is_secret_header` and `redact_cookies_from_header` with `crate::constants::headers` constants.
    - Replace cookie/auth pattern literals in `redact_cookie_like_substrings` with `crate::constants::har` constants.

    In `src/transient_400.rs`:
    - Replace `"\"er\""`, `"\"di\""`, `"\"af.httprm\""`, `"Sign in - Google Accounts"` with `crate::constants::transient` constants.

    In `src/metrics.rs`:
    - Replace `"gemini_sdk.requests"`, `"gemini_sdk.request_latency"`, `"gemini_sdk.retries"`, `"status"`, `"operation"` with `crate::constants::tracing` constants.

    In `src/attestation.rs`:
    - Replace CDP method strings (`"Runtime.enable"`, etc.) with `crate::constants::attestation` constants.
    - Replace `"https://gemini.google.com/app?hl=en"` with the template constant formatted with `"en"`.
    - Replace selectors and Chrome flags with constants.
    - Replace `".google.com"`, `"/"`, `"/tmp/gemini-sdk-chrome-profile"`, `"en"` with constants.
    - Keep timeout `Duration::from_secs(60)` but derive from `NAVIGATE_TIMEOUT_SECONDS`.

    In `src/tool.rs`:
    - Replace `"type"`, `"object"`, `"properties"`, `"required"`, `"name"`, `"parameters"` in `tool_declaration` and tests with `crate::constants::tool_schema` constants.

    Do NOT change behavior.
  </action>
  <verify>
    <automated>cargo test --lib har --lib transient_400 --lib metrics --lib attestation --lib tool -- --nocapture</automated>
  </verify>
  <done>
    - All affected unit tests pass.
    - Centralized literals no longer appear inline in the affected modules (verified by grep).
  </done>
</task>

<task type="auto">
  <name>Task 4: Quality gates and final verification</name>
  <files>src/constants.rs, src/client.rs, src/upload.rs, src/har.rs, src/transient_400.rs, src/metrics.rs, src/attestation.rs, src/tool.rs</files>
  <action>
    Run the full quality gate suite and fix any warnings or failures:

    1. `cargo test --all-targets`
    2. `cargo clippy --all-targets -- -D warnings`
    3. `cargo doc --no-deps`

    Address missing-docs or clippy issues. Verify with greps that the centralized infrastructure literals no longer appear inline in production files, except as constant definitions.
  </action>
  <verify>
    <automated>cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps</automated>
  </verify>
  <done>
    - `cargo test --all-targets` passes.
    - `cargo clippy --all-targets -- -D warnings` passes.
    - `cargo doc --no-deps` builds with no warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header constants → HTTP requests | Same header values, only source changes. |
| HAR constants → audit output | Same redaction behavior, only source changes. |
| CDP constants → attestation | Same browser protocol, only source changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | HAR constants | low | accept | Constants are metadata; redaction values (`<redacted>`) are already safe to expose. |
| T-15-02 | Tampering | Header/attestation constants | low | mitigate | Centralization reduces accidental drift; no runtime behavior change. |
| T-15-SC | Tampering | No new packages installed | — | mitigate | No package-manager install tasks. |
</threat_model>

<verification>
- Unit tests in `src/har.rs`, `src/transient_400.rs`, `src/metrics.rs`, `src/attestation.rs`, and `src/tool.rs` verify behavior is unchanged.
- Full suite passes: `cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`.
- Targeted greps confirm centralized infrastructure literals are no longer inlined in production code.
</verification>

<success_criteria>
- Header/base URL, HAR, transient markers, tracing/metrics, attestation, and tool schema strings are centralized.
- All infrastructure modules consume the new constants.
- Quality gates are green.
</success_criteria>

<output>
Create `.planning/phases/15-infrastructure-constants/15-01-SUMMARY.md` when done.
</output>
