---
phase: 14-model-chat-upload-constants
plan: 01
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - src/models.rs
  - src/chat.rs
  - src/proto/slots.rs
  - src/upload.rs
  - src/constants.rs
  - src/lib.rs
autonomous: true
requirements:
  - MAINT-06
  - MAINT-07
  - MAINT-08
  - MAINT-09

must_haves:
  truths:
    - "Model category enum values, display strings, and derivation keywords are centralized"
    - "Chat message roles use constants; no inline 'user'/'model' strings remain in production code"
    - "MIME types for images, audio, video, PDF, and media validation are centralized"
    - "Upload endpoint strings and upload command headers are centralized"
    - "cargo test --all-targets, cargo clippy --all-targets -- -D warnings, and cargo doc --no-deps pass"
  artifacts:
    - path: "src/models.rs"
      provides: "ModelCategory constants and derivation helpers"
    - path: "src/chat.rs"
      provides: "Chat message role constants"
    - path: "src/proto/slots.rs"
      provides: "MIME-to-extension mapping using centralized MIME constants"
    - path: "src/upload.rs"
      provides: "Upload URL/header constants and media validation"
    - path: "src/constants.rs"
      provides: "mime, model, role, and upload modules"
  key_links:
    - from: "src/constants.rs"
      to: "src/models.rs"
      via: "derive_category uses centralized keywords"
    - from: "src/constants.rs"
      to: "src/chat.rs"
      via: "ChatMessage::user/model use role constants"
    - from: "src/constants.rs"
      to: "src/proto/slots.rs"
      via: "derive_attachment_filename matches on MIME constants"
    - from: "src/constants.rs"
      to: "src/upload.rs"
      via: "Upload endpoint and header constants"
---

<objective>
Centralize model/category strings, chat message roles, MIME types, and upload endpoint/header strings. Extend `src/constants.rs` with `mime`, `model`, `role`, and `upload` modules and refactor `src/models.rs`, `src/chat.rs`, `src/proto/slots.rs`, and `src/upload.rs` to consume them without changing public API behavior.

Purpose: Eliminate duplication in media handling, model categorization, and chat message construction.
Output: Extended constants module, refactored model/chat/upload/slots code, and green quality gates.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-protocol-constants/13-01-SUMMARY.md
@src/models.rs
@src/chat.rs
@src/proto/slots.rs
@src/upload.rs
@src/constants.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/constants.rs with MIME, model, role, and upload constants</name>
  <files>src/constants.rs</files>
  <action>
    Extend `src/constants.rs` with four new modules. All items are `pub(crate) const` with doc comments.

    - `pub mod mime`:
      - `pub(crate) const PNG: &str = "image/png";`
      - `pub(crate) const JPEG: &str = "image/jpeg";`
      - `pub(crate) const WEBP: &str = "image/webp";`
      - `pub(crate) const GIF: &str = "image/gif";`
      - `pub(crate) const PDF: &str = "application/pdf";`
      - `pub(crate) const MP3: &str = "audio/mp3";`
      - `pub(crate) const MPEG_AUDIO: &str = "audio/mpeg";`
      - `pub(crate) const WAV: &str = "audio/wav";`
      - `pub(crate) const OGG_AUDIO: &str = "audio/ogg";`
      - `pub(crate) const MP4_VIDEO: &str = "video/mp4";`
      - `pub(crate) const WEBM_VIDEO: &str = "video/webm";`
      - `pub(crate) const QUICKTIME: &str = "video/quicktime";`
      - `pub(crate) const JSON: &str = "application/json";`
      - `pub(crate) const PLAIN_TEXT: &str = "text/plain";`
      - `pub(crate) const FORM_URLENCODED: &str = "application/x-www-form-urlencoded;charset=UTF-8";`
      - `pub(crate) const JSON_PROTOBUF: &str = "application/json+protobuf";`
      - Helper `pub(crate) fn supported_image_mime_types() -> &'static [&'static str]` returning PNG, JPEG, WEBP, GIF.
      - Helper `pub(crate) fn supported_audio_mime_types() -> &'static [&'static str]` returning MP3, MPEG_AUDIO, WAV, OGG_AUDIO.
      - Helper `pub(crate) fn supported_video_mime_types() -> &'static [&'static str]` returning MP4_VIDEO, WEBM_VIDEO, QUICKTIME.

    - `pub mod roles`:
      - `pub(crate) const USER: &str = "user";`
      - `pub(crate) const MODEL: &str = "model";`

    - `pub mod model_keywords`:
      - `pub(crate) const LITE: &str = "lite";`
      - `pub(crate) const THINKING: &str = "thinking";`
      - `pub(crate) const DEEP: &str = "deep";`
      - `pub(crate) const PRO: &str = "pro";`
      - `pub(crate) const AUTO: &str = "auto";`
      - `pub(crate) const FLASH: &str = "flash";`
      - Display-title constants: `pub(crate) const TITLE_FLASH: &str = "Flash";`, `TITLE_PRO: &str = "Pro";`.

    - `pub mod upload`:
      - `pub(crate) const UPLOAD_COMMAND_START: &str = "start";`
      - `pub(crate) const UPLOAD_COMMAND_FINALIZE: &str = "finalize";` (if used)
      - `pub(crate) const X_GOOG_UPLOAD_COMMAND: &str = "x-goog-upload-command";`
      - `pub(crate) const X_GOOG_UPLOAD_HEADER_CONTENT_LENGTH: &str = "x-goog-upload-header-content-length";`
      - `pub(crate) const X_GOOG_UPLOAD_PROTOCOL: &str = "x-goog-upload-protocol";`
      - `pub(crate) const X_GOOG_UPLOAD_URL: &str = "x-goog-upload-url";`
      - `pub(crate) const X_TENANT_ID: &str = "x-tenant-id";`
      - `pub(crate) const PUSH_ID_HEADER: &str = "push-id";`
      - `pub(crate) const RESUMABLE_PROTOCOL: &str = "resumable";`
      - `pub(crate) const UPLOAD_PATH: &str = "/upload/";`
      - `pub(crate) const BARD_STORAGE_TENANT: &str = "bard-storage";`

    Do NOT change existing behavior; only centralize strings.
  </action>
  <verify>
    <automated>cargo check --all-targets</automated>
  </verify>
  <done>
    - `src/constants.rs` compiles with the new modules.
    - All constants have doc comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor model and chat modules to use constants</name>
  <files>src/models.rs, src/chat.rs</files>
  <action>
    In `src/models.rs`:
    - Update `derive_category` to use `crate::constants::model_keywords` for `"lite"`, `"thinking"`, `"deep"`, `"pro"`, `"auto"`.
    - Replace test literal `"Flash"` / `"Pro"` with `TITLE_FLASH` / `TITLE_PRO` from `constants::model_keywords`.
    - Keep enum value integers as they are protocol numeric values, but add a doc comment on `as_enum_value` noting the mapping is fixed by Google.

    In `src/chat.rs`:
    - Replace `"user"` and `"model"` literals in `ChatMessage::user`, `ChatMessage::model`, and any other production methods with `crate::constants::roles::USER` and `MODEL`.
    - Update doc comments for `ChatMessage::text` and `role` field to reference the role constants.

    Do NOT change `pub role: String` type; callers can still supply arbitrary strings.
  </action>
  <verify>
    <automated>cargo test --lib models --lib chat -- --nocapture</automated>
  </verify>
  <done>
    - Model and chat unit tests pass.
    - No inline `"user"` or `"model"` literals remain in `src/chat.rs` production code (tests may still use them via constants).
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor slots and upload modules to use MIME and upload constants</name>
  <files>src/proto/slots.rs, src/upload.rs</files>
  <action>
    In `src/proto/slots.rs`:
    - Replace MIME string literals in `derive_attachment_filename` with `crate::constants::mime::*` constants.
    - Replace test literals `"image/png"`, `"application/pdf"`, etc., with MIME constants.
    - Use the extension `"bin"` fallback as a local `const DEFAULT_EXTENSION: &str = "bin";`.

    In `src/upload.rs`:
    - Replace `PUSH_UPLOAD_URL` construction to use `crate::constants::urls::PUSH_UPLOAD_BASE_URL` + `crate::constants::upload::UPLOAD_PATH`.
    - Replace upload header name literals (`x-goog-upload-command`, etc.) with `constants::upload` constants.
    - Replace `"bard-storage"`, `"resumable"`, `"start"` with constants.
    - Replace MIME literals in `is_allowed_media_type` tests with `constants::mime` constants.
    - Keep the two `USER_AGENT` duplicates in `upload.rs` for now; they are covered in Phase 15.

    Do NOT change the upload protocol logic.
  </action>
  <verify>
    <automated>cargo test --lib proto::slots --lib upload -- --nocapture</automated>
  </verify>
  <done>
    - Slots and upload unit tests pass.
    - No inline MIME type literals remain in `src/proto/slots.rs` or `src/upload.rs`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Quality gates and final verification</name>
  <files>src/constants.rs, src/models.rs, src/chat.rs, src/proto/slots.rs, src/upload.rs</files>
  <action>
    Run the full quality gate suite and fix any warnings or failures:

    1. `cargo test --all-targets`
    2. `cargo clippy --all-targets -- -D warnings`
    3. `cargo doc --no-deps`

    Address missing-docs or clippy issues. Verify with greps that the centralized literals no longer appear inline in production files:
    - `"user"` and `"model"` only as constants or in `#[cfg(test)]` blocks in `src/chat.rs`.
    - `"image/png"`, `"audio/mp3"`, `"video/mp4"`, etc., only in `src/constants.rs`.
    - `"x-goog-upload-command"`, `"x-goog-upload-protocol"`, `"bard-storage"`, `"resumable"` only in `src/constants.rs`.
  </action>
  <verify>
    <automated>cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps</automated>
  </verify>
  <done>
    - `cargo test --all-targets` passes.
    - `cargo clippy --all-targets -- -D warnings` passes.
    - `cargo doc --no-deps` builds with no warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIME constants → upload validation | Same validation logic, only string source changes. |
| Role constants → chat payload construction | Same WIZ request shape, only string source changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | MIME type constants | low | mitigate | Centralized constants reduce accidental drift; no runtime behavior change. |
| T-14-02 | Information Disclosure | Model keyword constants | low | accept | Keywords are public UI labels; no secrets. |
| T-14-SC | Tampering | No new packages installed | — | mitigate | No package-manager install tasks. |
</threat_model>

<verification>
- Unit tests in `src/models.rs`, `src/chat.rs`, `src/proto/slots.rs`, and `src/upload.rs` verify behavior is unchanged.
- Full suite passes: `cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`.
- Targeted greps confirm centralized literals are no longer inlined in production code.
</verification>

<success_criteria>
- Model/category strings, chat roles, MIME types, and upload strings are centralized.
- `src/models.rs`, `src/chat.rs`, `src/proto/slots.rs`, and `src/upload.rs` consume the new constants.
- Quality gates are green.
</success_criteria>

<output>
Create `.planning/phases/14-model-chat-upload-constants/14-01-SUMMARY.md` when done.
</output>
