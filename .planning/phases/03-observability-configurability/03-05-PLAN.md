---
phase: 03-observability-configurability
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/session.rs
  - tests/fixtures/wiz_aliases/
  - tests/unit/session_extractors.rs
autonomous: true
requirements:
  - PROTO-03
must_haves:
  truths:
    - `extract_snlim0e`, `extract_build_label`, `extract_session_id`, and `extract_push_id` try a list of known aliases before giving up.
    - Each extractor validates the extracted value with an existing or new shape checker.
    - Fixture tests cover each alias shape documented in CONTEXT.md.
    - Fallback order is documented in a comment per extractor.
  artifacts:
    - src/session.rs
    - tests/fixtures/wiz_aliases/
    - tests/unit/session_extractors.rs
  key_links:
    - HTML extractors -> `init_session` and `verify_signed_in`
---

<objective>
Make HTML session extraction resilient to Google's layout changes by falling back through multiple known keys/selectors.

Purpose: PROTO-03 requires robust HTML extraction. The current extractors already have limited fallbacks; this plan expands them systematically and adds fixture coverage.
Output: Refactored extractors with alias lists and unit tests for each fallback shape.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-observability-configurability/03-CONTEXT.md
@.planning/phases/03-observability-configurability/03-RESEARCH.md
@.planning/phases/03-observability-configurability/03-PATTERNS.md
@src/session.rs
</context>

<feature>
  <name>HTML extractor fallback chain</name>
  <files>src/session.rs, tests/unit/session_extractors.rs, tests/fixtures/wiz_aliases/</files>
  <behavior>
    - `SNlM0e` aliases: `SNlM0e`, `SnlM0e`, `snlM0e`.
    - `cfb2h` (build label) aliases: `cfb2h`, `build_label` plus the existing bare-prefix fallback.
    - `FdrFJe` (session id) aliases: `FdrFJe`, `f.sid`, `session_id`.
    - Push id aliases: `qKIAYe`, `KnDnFf`, `push_id`.
    - WAA fingerprint aliases keep the existing `"Pro"` block scan but also try an explicit key `VfueOe` if documented in spike findings.
    - Each alias is tried inside `window.WIZ_global_data` first, then in the whole body as a last resort.
  </behavior>
  <implementation>
    Add a private helper `try_extract_value(body: &str, block: Option<&str>, keys: &[&str], validator: fn(&str) -> Option<String>) -> Option<String>` that tries each key in order. Refactor each extractor to use the helper with its alias list and validator. Add fixture files under `tests/fixtures/wiz_aliases/` for each alias shape and tests in `tests/unit/session_extractors.rs`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: Add fixture files and failing extractor tests</name>
  <files>tests/fixtures/wiz_aliases/, tests/unit/session_extractors.rs</files>
  <behavior>
    - For each alias listed in the feature, create a minimal HTML fixture under `tests/fixtures/wiz_aliases/{extractor}_{alias}.txt` containing `window.WIZ_global_data = {"{alias}":"..."};`.
    - Write tests in `tests/unit/session_extractors.rs` that load each fixture and assert the extractor returns the expected value.
  </behavior>
  <action>
    Create fixture files and `tests/unit/session_extractors.rs`. Tests will fail because `try_extract_value` and alias lists are not implemented yet (existing extractors may pass primary fixtures but fail alias fixtures).
  </action>
  <verify>
    <automated>cargo test --test session_extractors --lib 2>&1 | grep -E "(FAILED|failures)" | head -10</automated>
  </verify>
  <done>Fixture files and test file exist; alias fixture tests fail (expected RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: Implement fallback extractor helper</name>
  <files>src/session.rs</files>
  <action>
    Add `fn try_extract_value(block: Option<&str>, body: &str, keys: &[&str], validate: impl Fn(&str) -> Option<String>) -> Option<String>` that iterates keys. For each key, first search the WIZ block (if present), then the whole body, and return the first value that passes `validate`. Refactor `extract_snlim0e`, `extract_build_label`, `extract_session_id`, and `extract_push_id` to use the helper with their alias lists and existing validators. Add a short comment above each extractor listing the alias order and the spike finding that motivated it.
  </action>
  <verify>
    <automated>cargo test --test session_extractors 2>&1 | tail -20</automated>
  </verify>
  <done>All alias fixture tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>REFACTOR: Validate fallback order and add regression tests</name>
  <files>src/session.rs, tests/unit/session_extractors.rs</files>
  <action>
    Add tests that verify the fallback order: a fixture with a valid secondary alias but invalid primary alias should return the secondary value. Add a test that a fixture with no known aliases returns `None`. Ensure `extract_from_app_html` still populates all fields correctly with the real `wiz_global_data.txt` fixture.
  </action>
  <verify>
    <automated>cargo test --test session_extractors && cargo test --all-targets --quiet && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Fallback order is tested; all tests and clippy pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted HTML -> SDK | Extractors parse untrusted Google HTML and must reject malformed values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Spoofing | HTML extractor aliases | medium | mitigate | Each value passes a shape validator before acceptance; whole-body fallback is only used when WIZ block fails. |
| T-03-12 | Information Disclosure | Verbose extractor logging | low | mitigate | Do not log extracted tokens or values at info level; use debug only with redaction. |
</threat_model>

<verification>
- `cargo test --test session_extractors` passes.
- `cargo test --all-targets` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` builds without warnings.
</verification>

<success_criteria>
- All four extractors use a shared alias fallback helper.
- Alias order is documented in comments.
- Fixture tests cover each alias shape.
- Fallback order regression tests pass.
- `extract_from_app_html` still works with the real fixture.
</success_criteria>

<output>
Create `.planning/phases/03-observability-configurability/03-05-SUMMARY.md` when done.
</output>
