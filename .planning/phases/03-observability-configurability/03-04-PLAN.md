---
phase: 03-observability-configurability
plan: 04
type: tdd
wave: 2
depends_on:
  - 03-03
files_modified:
  - src/upload.rs
  - src/chat.rs
  - src/client.rs
  - src/lib.rs
  - tests/unit/upload_progress.rs
autonomous: true
requirements:
  - MEDIA-02
must_haves:
  truths:
    - A public `UploadEvent` enum exists with `Progress { uploaded: u64, total: Option<u64> }` and `Complete { attachment: WebAttachment }` variants.
    - `GeminiClient::upload_with_progress(...)` returns a `futures::Stream<Item = Result<UploadEvent>>`.
    - The stream yields at least one `Progress` event and a final `Complete` event for a successful upload.
    - Existing `upload_attachments` path remains unchanged.
  artifacts:
    - src/upload.rs
    - src/chat.rs
    - src/client.rs
    - src/lib.rs
    - tests/unit/upload_progress.rs
  key_links:
    - `upload_with_progress` -> `upload_file` steps
    - `UploadEvent` -> `futures::Stream` consumer
---

<objective>
Expose upload progress as a `futures::Stream` so callers can observe multi-step resumable uploads.

Purpose: MEDIA-02 requires observable upload progress. A stream fits the existing async Rust patterns used by `generate_stream`.
Output: `UploadEvent` enum, `upload_with_progress` method, and unit tests verifying event sequence.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-observability-configurability/03-CONTEXT.md
@.planning/phases/03-observability-configurability/03-RESEARCH.md
@.planning/phases/03-observability-configurability/03-PATTERNS.md
@.planning/phases/02-reliability-protocol-hardening/02-03-SUMMARY.md
@src/upload.rs
@src/chat.rs
@src/client.rs
@src/lib.rs
</context>

<feature>
  <name>Upload progress stream</name>
  <files>src/upload.rs, src/chat.rs, src/client.rs, src/lib.rs, tests/unit/upload_progress.rs</files>
  <behavior>
    - `UploadEvent::Progress { uploaded, total }` reports bytes uploaded and total size.
    - `UploadEvent::Complete { attachment: WebAttachment }` reports the finished attachment.
    - `GeminiClient::upload_with_progress(filename, mime_type, bytes)` returns a stream.
    - The stream uses the existing resumable upload start/finalize steps and yields events between them.
    - Cancellation before `Complete` is allowed; documentation notes that server-side state is best-effort.
  </behavior>
  <implementation>
    Add `UploadEvent` enum to `src/upload.rs` (or a new public module if needed). Refactor `upload_file` into `start_upload` and `finalize_upload` helpers so progress can be yielded between them. Add `pub fn upload_with_progress(&self, filename: impl Into<String>, mime_type: impl Into<String>, bytes: Vec<u8>) -> impl Stream<Item = Result<UploadEvent>> + Send + '_` on `GeminiClient` that clones the client and uses `async_stream::stream!` to yield `Progress` at start and after each chunk, then `Complete`. Re-export `UploadEvent` from `src/lib.rs`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>RED: Add failing upload progress tests</name>
  <files>tests/unit/upload_progress.rs</files>
  <behavior>
    - Test `upload_progress_yields_progress_and_complete`: mock the Google upload start and finalize endpoints, consume the stream, and assert at least one `Progress` event and one final `Complete` event with a non-empty reference.
    - Test `upload_progress_reports_total_size`: assert the first `Progress` event has `total == Some(bytes.len() as u64)`.
  </behavior>
  <action>
    Create `tests/unit/upload_progress.rs` with the two tests. They will fail to compile because `UploadEvent` and `upload_with_progress` do not exist yet.
  </action>
  <verify>
    <automated>cargo test --test upload_progress --lib 2>&1 | grep -E "(error|FAILED)" | head -5</automated>
  </verify>
  <done>`tests/unit/upload_progress.rs` exists and fails due to missing API (expected RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: Implement UploadEvent and upload_with_progress</name>
  <files>src/upload.rs, src/chat.rs, src/client.rs, src/lib.rs</files>
  <action>
    In `src/upload.rs`:
    - Add `#[derive(Debug, Clone)] #[non_exhaustive] pub enum UploadEvent { Progress { uploaded: u64, total: Option<u64> }, Complete { attachment: WebAttachment } }`.
    - Split `upload_file` into `start_upload` (returns upload URL) and `finalize_upload` (returns reference) helpers.
    - Keep `upload_file` as an async wrapper that calls both helpers for backward compatibility.
    In `src/client.rs`:
    - Add `pub fn upload_with_progress(&self, filename: impl Into<String>, mime_type: impl Into<String>, bytes: Vec<u8>) -> impl Stream<Item = Result<UploadEvent>> + Send + '_` using `async_stream::stream!`. Yield `Progress { uploaded: 0, total: Some(total) }`, then call `start_upload`, yield `Progress { uploaded: 0, total }` again, call `finalize_upload`, yield `Complete { attachment: WebAttachment { reference, mime_type, filename } }`.
    In `src/lib.rs`: re-export `UploadEvent`.
  </action>
  <verify>
    <automated>cargo test --test upload_progress 2>&1 | tail -20</automated>
  </verify>
  <done>Both upload progress tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>REFACTOR: Share upload logic between old and new paths</name>
  <files>src/upload.rs</files>
  <action>
    Ensure `upload_file` and `upload_with_progress` share the same `start_upload`/`finalize_upload` helpers so bug fixes apply to both. Remove any duplicated header construction.
  </action>
  <verify>
    <automated>cargo test --test upload_progress && cargo test --all-targets --quiet && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Upload logic is shared; all tests and clippy pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> caller stream consumer | Progress events leave the SDK via the `Stream` trait. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Information Disclosure | `UploadEvent::Progress` | low | accept | Progress only exposes byte counts, no content. |
| T-03-09 | Denial of Service | Slow stream consumer | medium | mitigate | Stream is pull-based; backpressure is handled by `futures` stream polling. |
| T-03-10 | Tampering | Partial upload state | low | accept | Document that dropping the stream before `Complete` leaves server-side state undefined. |
</threat_model>

<verification>
- `cargo test --test upload_progress` passes.
- `cargo test --all-targets` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` builds without warnings.
</verification>

<success_criteria>
- `UploadEvent` enum is public and `#[non_exhaustive]`.
- `upload_with_progress` returns a `futures::Stream` of `Result<UploadEvent>`.
- The stream yields `Progress` and `Complete` events.
- Existing `upload_attachments` still works unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-observability-configurability/03-04-SUMMARY.md` when done.
</output>
