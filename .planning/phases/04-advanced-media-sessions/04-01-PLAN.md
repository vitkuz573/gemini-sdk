---
phase: 04-advanced-media-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chat.rs
  - src/upload.rs
  - src/proto/slots.rs
  - src/lib.rs
autonomous: true
requirements:
  - MEDIA-03
must_haves:
  truths:
    - Public API exposes AudioSource, VideoSource, ContentPart::Audio, and ContentPart::Video.
    - Inline audio/video bytes are base64-decoded, uploaded, and referenced in StreamGenerate slot 0 with the same attachment shape as images.
    - Unsupported MIME types are rejected with a typed error before any network call.
  artifacts:
    - src/chat.rs (AudioSource, VideoSource, ContentPart variants, PreparedRequest fields)
    - src/upload.rs (MIME allowlist, media-agnostic upload_attachments)
    - src/proto/slots.rs (derive_attachment_filename extensions)
    - src/lib.rs (re-exports)
  key_links:
    - ContentPart::Audio/Video -> PreparedRequest.inline_audio/inline_video -> upload_attachments -> WebAttachment -> build_slot0
---

<objective>
Add audio and video upload support that mirrors the existing image path. This plan delivers MEDIA-03 by extending content parts, request preparation, upload, and slot-0 construction.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-advanced-media-sessions/04-CONTEXT.md
@.planning/phases/04-advanced-media-sessions/04-RESEARCH.md
@src/chat.rs
@src/upload.rs
@src/proto/slots.rs
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AudioSource and VideoSource enums and ContentPart variants</name>
  <files>src/chat.rs, src/lib.rs</files>
  <read_first>
    - src/chat.rs (ImageSource, ContentPart, PreparedRequest definitions)
    - src/lib.rs (current re-exports)
  </read_first>
  <action>
    In src/chat.rs, add public `AudioSource` and `VideoSource` enums with `InlineData { mime_type: String, data: String }` and `Url { url: String }` variants, mirroring `ImageSource` exactly. Add `ContentPart::Audio(AudioSource)` and `ContentPart::Video(VideoSource)` variants. Add `ChatMessage::with_audio(role, source)` and `ChatMessage::with_video(role, source)` convenience constructors. Extend `PreparedRequest` with `inline_audio: Vec<(String, String)>` and `inline_video: Vec<(String, String)>` fields. Extend `prepare_request` so it extracts inline audio/video data from `new_message.parts` into those vectors, ignoring `Url` variants and `Text`/`Thinking` parts. Update `extract_prompt` to ignore the new part variants. Re-export `AudioSource` and `VideoSource` from src/lib.rs.
  </action>
  <verify>
    <automated>cargo check</automated>
    <automated>grep -q "pub enum AudioSource" src/chat.rs && grep -q "pub enum VideoSource" src/chat.rs && grep -q "Audio(AudioSource)" src/chat.rs && grep -q "Video(VideoSource)" src/chat.rs</automated>
  </verify>
  <done>
    - src/chat.rs compiles with the new enums, variants, constructors, and PreparedRequest fields.
    - src/lib.rs re-exports AudioSource and VideoSource.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend upload flow with MIME allowlist and media-agnostic attachment handling</name>
  <files>src/upload.rs, src/proto/slots.rs</files>
  <read_first>
    - src/upload.rs (upload_attachments, upload_file, UploadEvent)
    - src/proto/slots.rs (derive_attachment_filename, build_slot0)
  </read_first>
  <action>
    In src/upload.rs, add a private `is_allowed_media_type(mime_type: &str) -> bool` helper that accepts audio/mp3, audio/mpeg, audio/wav, audio/ogg, video/mp4, video/webm, video/quicktime, and image/*. Update `upload_attachments` to iterate over `prepared.inline_images`, `prepared.inline_audio`, and `prepared.inline_video`, rejecting any inline entry whose MIME type is not allowed with `Error::bad_request("unsupported media type: {mime_type}")`. Keep the existing resumable upload logic unchanged. In src/proto/slots.rs, extend `derive_attachment_filename` to map the new audio/video MIME types to file extensions (mp3, wav, ogg, mp4, webm, mov). No changes are needed to `build_slot0`; the existing attachment shape is media-agnostic.
  </action>
  <verify>
    <automated>cargo check</automated>
    <automated>cargo test -p gemini-sdk upload::tests --lib 2>/dev/null || cargo test --lib upload</automated>
  </verify>
  <done>
    - upload_attachments compiles and handles audio/video inline data.
    - Unsupported MIME types are rejected with a typed error.
    - derive_attachment_filename returns correct extensions for audio/video types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for MIME allowlist and filename derivation</name>
  <files>src/upload.rs, src/proto/slots.rs</files>
  <read_first>
    - src/upload.rs (existing tests, if any)
    - src/proto/slots.rs (derive_attachment_filename, existing tests)
  </read_first>
  <action>
    In src/upload.rs, add `#[cfg(test)] mod tests` if absent. Add tests that: pass allowed audio/video MIME types through `is_allowed_media_type`; reject disallowed MIME types; verify that `upload_attachments` returns an error for an unsupported MIME type. In src/proto/slots.rs, add tests for `derive_attachment_filename` covering audio/mp3 -> mp3, audio/wav -> wav, video/mp4 -> mp4, video/webm -> webm, and the existing image/application cases.
  </action>
  <verify>
    <automated>cargo test --lib upload::tests</automated>
    <automated>cargo test --lib proto::slots::tests</automated>
  </verify>
  <done>
    - New unit tests pass for MIME allowlist and filename derivation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> SDK | Audio/video bytes are caller-supplied; MIME type is caller-asserted. |
| SDK -> Google upload endpoint | Upload URL origin is validated against `*.google.com`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | MIME type allowlist | medium | mitigate | Reject unsupported MIME types before network upload. |
| T-04-02 | Information Disclosure | upload response | low | accept | Existing code returns only attachment reference; no secret leakage. |
</threat_model>

<verification>
  <automated>cargo test --lib</automated>
  <automated>cargo clippy --all-targets -- -D warnings</automated>
  <automated>cargo doc --no-deps</automated>
</verification>

<success_criteria>
  - `AudioSource`, `VideoSource`, `ContentPart::Audio`, and `ContentPart::Video` are public and documented.
  - Inline audio/video data flows through `PreparedRequest` to `upload_attachments` and into slot 0 with the correct attachment shape.
  - Unsupported MIME types are rejected before any network call.
  - All existing tests still pass; new tests cover MIME allowlist and filename derivation.
</success_criteria>

<output>
Create `.planning/phases/04-advanced-media-sessions/04-01-SUMMARY.md` when done.
</output>
