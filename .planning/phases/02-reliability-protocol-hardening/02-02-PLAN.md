---
phase: 02-reliability-protocol-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/proto/indices.rs
  - src/proto/slots.rs
  - src/proto/parser.rs
  - src/proto/mod.rs
  - tests/proto_tests.rs
  - tests/fixtures
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-04
must_haves:
  truths:
    - All magic indices used to build and parse the 97-slot WIZ array live as named constants in `src/proto/indices.rs`.
    - Parser tests cover every documented response shape with dedicated fixture files.
    - No `.unwrap()` or `.expect()` remains in `src/proto/slots.rs` or `src/proto/parser.rs`; all failure paths return `Error::Parse`.
  artifacts:
    - src/proto/indices.rs with builder and parser constants.
    - src/proto/slots.rs using constants from indices.rs and no raw magic numbers.
    - src/proto/parser.rs using constants from indices.rs and returning structured errors.
    - tests/fixtures/*.json (or .txt) for simple text, thinking, concatenated chunks, first-turn meta, continuation key 21, error-code wrapper.
    - tests/proto_tests.rs with tests for the new fixtures.
  key_links:
    - indices.rs constants -> slots.rs builder assignments.
    - indices.rs constants -> parser.rs extraction logic.
    - parser.rs -> Error::Parse for malformed protocol data.
---

<objective>
Centralize WIZ protocol constants, harden parsing against malformed responses, and expand test coverage for all documented response shapes.

Purpose: Magic indices and silent panics are the biggest source of protocol drift fragility. This plan makes the protocol layer explicit and defensively correct.
Output: A single source of truth for slot/part indices, comprehensive parser fixtures, and no panic paths in protocol code.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-reliability-protocol-hardening/02-CONTEXT.md
@.planning/phases/02-reliability-protocol-hardening/02-RESEARCH.md
@.planning/phases/02-reliability-protocol-hardening/02-PATTERNS.md
@.planning/codebase/CONCERNS.md
@src/proto/slots.rs
@src/proto/parser.rs
@src/proto/mod.rs
@tests/proto_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/proto/indices.rs and replace magic numbers in slots.rs and parser.rs</name>
  <files>src/proto/indices.rs, src/proto/slots.rs, src/proto/parser.rs, src/proto/mod.rs</files>
  <behavior>
    - Test 1: `grep -R '\[0-9\]\{1,2\}=' src/proto/slots.rs src/proto/parser.rs` (excluding comments and the 97 count) returns zero raw slot assignments.
    - Test 2: `cargo test --test proto_tests` passes after the refactor.
    - Test 3: `cargo clippy --all-targets -- -D warnings` passes.
  </behavior>
  <action>
    Create src/proto/indices.rs. Define public `const` items grouped into `pub mod builder` (for indices used when building the 97-slot request list) and `pub mod parser` (for indices used when parsing responses). Include at least: builder::SLOT_PROMPT, SLOT_LANGUAGE, SLOT_CONVERSATION_STATE, SLOT_WAA_TOKEN, SLOT_NONCE, SLOT_CATEGORY, SLOT_REQUEST_UUID, SLOT_THINKING_LEVEL, SLOT_FINAL_FLAG; parser::PART_TEXT, PART_THINKING, PART_ID, CANDIDATE_PARTS, CONVERSATION_IDS, META_TOKEN_KEY_26, META_TOKEN_KEY_21, RPC_ID, PAYLOAD, MAIN_ENTRY_MIN_LEN, WAA_CONTEXT_LEN. Document each constant with a one-line rationale. In src/proto/mod.rs, add `pub mod indices;` and re-export the module if needed. In src/proto/slots.rs, replace every raw index in `build_inner_req_list`, `build_fallback_base`, `build_slot0`, and `ConversationState::to_slot2` with the named constants from `indices.rs`. In src/proto/parser.rs, replace `PART_TEXT_INDEX`, `PART_THINKING_INDEX`, and all other numeric indices (e.g., 4 for parts, 1 for ids, 2 for payload string, 3 for alternate payload string, 15 for WAA context uuid, 17 for WAA context array length check) with the constants from `indices.rs`. Ensure no raw magic numbers remain except the documented `SLOT_COUNT = 97` and small loop literals.
  </action>
  <verify>
    <automated>cargo test --test proto_tests && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>All WIZ slot/part indices are centralized in src/proto/indices.rs and used by slots.rs and parser.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert remaining unwrap/expect paths in protocol code to Error::Parse</name>
  <files>src/proto/slots.rs, src/proto/parser.rs, src/proto/mod.rs</files>
  <behavior>
    - Test 1: `grep -nE '\.unwrap\(\)|\.expect\(' src/proto/slots.rs src/proto/parser.rs src/proto/mod.rs` returns only test-module lines.
    - Test 2: `cargo test --test proto_tests malformed_response_no_panic` passes.
    - Test 3: `cargo clippy --all-targets -- -D warnings` passes.
  </behavior>
  <action>
    Audit src/proto/slots.rs, src/proto/parser.rs, and src/proto/mod.rs for every `.unwrap()` and `.expect()` outside `#[cfg(test)]` modules. Replace each with `?` on a `Result` or an explicit `Error::parse(...)` return. Examples: `serde_json::to_string(...).unwrap_or_default()` in mod.rs can remain only if an empty default is truly safe; otherwise return `Error::parse`. In parser.rs, replace `.unwrap_or_default()` calls that hide missing data (e.g., response text default) with structured errors when the data is required. In slots.rs, ensure `build_inner_req_list` cannot panic on index out of bounds — it already starts with a 97-element vec, but verify `normalize_payload` truncation and array access use safe patterns. Add a unit test `malformed_response_no_panic` that feeds an invalid WIZ frame and asserts a structured `Error::Parse` is returned, not a panic.
  </action>
  <verify>
    <automated>cargo test --test proto_tests malformed_response_no_panic && cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>No production `.unwrap()`/`.expect()` remains in protocol code; malformed protocol data returns `Error::Parse`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add parser fixtures and tests for all documented response shapes</name>
  <files>tests/fixtures, tests/proto_tests.rs</files>
  <behavior>
    - Test 1: `cargo test --test proto_tests` covers simple text, thinking, concatenated chunks, first-turn meta, continuation key 21, and error-code wrapper.
    - Test 2: Each new fixture is loaded via `include_str!` and has a dedicated test function.
    - Test 3: `cargo test --all-targets --quiet` passes.
  </behavior>
  <action>
    Add fixture files under tests/fixtures/ for: (a) `chat_response_simple.json` — single text response, (b) `chat_response_thinking.json` — text + reasoning, (c) `chat_response_concatenated.json` already exists — verify it has a dedicated test, (d) `conversation_state_first_turn.json` — first-turn meta shape with {"26": "token"}, (e) `conversation_state_key_21.json` already exists — verify it has a dedicated test, (f) `bard_error_wrapper.json` — BardErrorInfo wrapper. For each new fixture, add a focused unit test in tests/proto_tests.rs that loads the fixture, calls the relevant parser function, and asserts the expected text/thinking/error/ids/token. Where fixtures already exist but lack dedicated tests, add the missing test. Ensure test names are descriptive, e.g., `parse_simple_text_response`, `parse_thinking_response`, `parse_concatenated_text_response`, `extract_first_turn_meta_token`, `extract_continuation_token_key_21`, `parse_chat_response_detects_bard_error_wrapper`.
  </action>
  <verify>
    <automated>cargo test --test proto_tests && cargo test --all-targets --quiet</automated>
  </verify>
  <done>Every documented response shape has a fixture file and a passing parser test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream WIZ response -> parser.rs | Untrusted JSON-like frames cross into the SDK; must not panic. |
| SDK -> upstream StreamGenerate | Request list must remain shape-compatible; constants keep builder/parser aligned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Denial of Service | `src/proto/parser.rs` | high | mitigate | Replace all `unwrap`/`expect` with `Error::Parse`; add malformed-frame test. |
| T-02-05 | Tampering | `src/proto/slots.rs` | medium | mitigate | Centralize indices so future edits cannot drift between builder and parser. |
| T-02-06 | Information Disclosure | `Error::Parse` messages | low | mitigate | Do not include raw cookie or auth values in parse error strings. |
| T-02-SC | Tampering | cargo installs | low | accept | No new external dependencies in this plan. |
</threat_model>

<verification>
- `cargo test --test proto_tests` passes.
- `cargo test --all-targets --quiet` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` produces no warnings.
</verification>

<success_criteria>
- `src/proto/indices.rs` exists and is imported by `src/proto/mod.rs`.
- `src/proto/slots.rs` and `src/proto/parser.rs` use only named constants from `indices.rs` (except `SLOT_COUNT = 97`).
- No `.unwrap()`/`.expect()` remains in production protocol code.
- Fixtures exist and have dedicated tests for simple text, thinking, concatenated chunks, first-turn meta, continuation token key 21, and error-code wrapper.
</success_criteria>

<output>
Create `.planning/phases/02-reliability-protocol-hardening/02-02-SUMMARY.md` when done.
</output>
