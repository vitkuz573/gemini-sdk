---
phase: 05-tools-auto-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/chat.rs
  - src/proto/slots.rs
  - src/proto/parser.rs
  - src/client.rs
  - tests/proto_tests.rs
  - tests/integration_tests.rs
autonomous: true
requirements:
  - ADV-01
must_haves:
  truths:
    - ChatBuilder::with_tools registers tools and passes them into GenerationConfig.
    - build_inner_req_list encodes tool declarations without breaking the existing slot shape when tools are absent.
    - Parser detects ToolCall parts in WIZ frames and falls back to text on unknown shapes.
    - GeminiClient::generate_with_tools performs tool invocation and a follow-up turn.
    - Tool-call recursion is capped to prevent infinite loops.
  artifacts:
    - src/chat.rs (with_tools, ContentPart variants, GenerationConfig tools field)
    - src/proto/slots.rs (tool encoding)
    - src/proto/parser.rs (tool-call parsing)
    - src/client.rs (generate_with_tools)
    - tests/proto_tests.rs (slot/parser tests)
    - tests/integration_tests.rs (wiremock round-trip)
  key_links:
    - ChatBuilder::with_tools -> GenerationConfig::tools -> PreparedRequest -> build_inner_req_list
    - build_inner_req_list -> slot 0 / dedicated tool slot
    - parse_response_parts -> ContentPart::ToolCall
    - GeminiClient::generate_with_tools -> Tool::invoke -> follow-up message with ContentPart::ToolResult
---

<objective>
Wire tool declarations into request building, parse tool calls from responses, and implement the round-trip generate_with_tools flow. This plan delivers the runtime ADV-01 behavior on top of the trait contracts from Plan 01.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-tools-auto-refresh/05-CONTEXT.md
@.planning/phases/05-tools-auto-refresh/05-RESEARCH.md
@.planning/phases/05-tools-auto-refresh/05-PATTERNS.md
@.planning/phases/05-tools-auto-refresh/05-01-PLAN.md
@src/chat.rs
@src/proto/slots.rs
@src/proto/parser.rs
@src/client.rs
@src/lib.rs
@tests/proto_tests.rs
@tests/integration_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ChatBuilder, GenerationConfig, and PreparedRequest for tools</name>
  <files>src/chat.rs</files>
  <read_first>
    - src/chat.rs (ChatBuilder, GenerationConfig, PreparedRequest, ContentPart)
  </read_first>
  <behavior>
    - ChatBuilder::with_tools(vec![tool]).tools() returns the same tools.
    - GenerationConfig::with_tools stores tools and serializes them as a field.
    - PreparedRequest carries an optional tool list derived from config.
    - ContentPart includes ToolCall { name, args } and ToolResult { name, result } variants.
    - Existing ContentPart match arms compile after adding variants.
  </behavior>
  <action>
    In src/chat.rs, add ToolCall and ToolResult variants to ContentPart using the types from src/tool.rs. Add tools: Option<Vec<Arc<dyn Tool>>> to GenerationConfig. Add with_tools builder method. Add tools: Option<Vec<Arc<dyn Tool>>> and refresh_on_auth_error: bool fields to ChatBuilder (the latter for Plan 03 wiring), with with_tools and with_refresh_on_auth_error methods. Update prepare_request to move tools into PreparedRequest. Update all match arms on ContentPart across the codebase (src/chat.rs, src/client.rs, src/proto/parser.rs) to handle the new variants; for now they should be ignored during prompt extraction and request preparation, and serialized appropriately when building follow-up turns.
  </action>
  <verify>
    <automated>cargo check --all-targets</automated>
    <automated>cargo test --lib chat</automated>
  </verify>
  <done>
    - ChatBuilder and GenerationConfig expose tool methods.
    - PreparedRequest carries tools.
    - All ContentPart matches compile.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode tool declarations in inner_req_list and parse tool calls</name>
  <files>src/proto/slots.rs, src/proto/parser.rs, tests/proto_tests.rs</files>
  <read_first>
    - src/proto/slots.rs (build_inner_req_list, build_slot0, PreparedRequest)
    - src/proto/parser.rs (parse_response_parts, extract_part_content)
    - src/proto/indices/* (slot and part indices)
  </read_first>
  <behavior>
    - build_inner_req_list with tools places a JSON array of tool declarations in a dedicated slot (slot 89 by convention) while leaving slot 0 shape unchanged.
    - When no tools are present, the inner request list matches the existing snapshots.
    - parse_response_parts extracts ContentPart::ToolCall from a WIZ frame containing a candidate part with a function-call shape.
    - Unknown part shapes fall back to text/thinking extraction without panicking.
  </behavior>
  <action>
    In src/proto/slots.rs, extend PreparedRequest (in src/chat.rs) and build_inner_req_list to accept tools. Add a named slot constant for tool declarations and set it to a JSON array of objects {name, parameters} derived from Tool::schema. Keep slot 0 unchanged when tools are absent. In src/proto/parser.rs, extend extract_part_content to detect a tool-call candidate shape (look for a part sub-array containing a function name and arguments object) and return a PartContent variant that yields ContentPart::ToolCall. Update parse_response_parts to include tool-call parts in the output, preserving accumulation by part id. Add tests to tests/proto_tests.rs covering tool encoding and tool-call parsing, and update any struct literals in tests to include new PreparedRequest fields.
  </action>
  <verify>
    <automated>cargo test --lib proto::slots::tests</automated>
    <automated>cargo test --lib proto::parser::tests</automated>
    <automated>cargo test --test proto_tests</automated>
  </verify>
  <done>
    - Tool declarations encode into inner_req_list.
    - Tool calls parse from synthetic WIZ frames.
    - Existing parser/slot tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement generate_with_tools round-trip</name>
  <files>src/client.rs, tests/integration_tests.rs</files>
  <read_first>
    - src/client.rs (generate_with_conversation, stream_generate_raw_with_prepared)
    - src/chat.rs (PreparedRequest, ChatMessage, ContentPart)
    - tests/integration_tests.rs (wiremock patterns)
  </read_first>
  <behavior>
    - GeminiClient::generate_with_tools sends a request with tool declarations, receives a ToolCall, invokes the matching tool, sends a follow-up turn with ToolResult, and returns the final ChatResponse.
    - If the model returns text instead of a tool call, the response is returned immediately.
    - Tool-call turns are capped at 5 by default to prevent loops.
    - Unknown tool names in a response return Error::Tool.
  </behavior>
  <action>
    In src/client.rs, add GeminiClient::generate_with_tools(message, tools, category, config) that builds a PreparedRequest with tools, calls generate_with_conversation, inspects the response parts for ContentPart::ToolCall, collects all calls, invokes matching registered tools in parallel, builds a follow-up user ChatMessage containing ContentPart::ToolResult parts, and recurses up to a configurable max_turns (default 5). If the final response contains only text/thinking, return it. If a tool name is not registered, return Error::Tool(ToolError::NotFound). Add a builder field max_tool_turns to GenerationConfig if useful. In tests/integration_tests.rs, add a wiremock-based test that mocks a StreamGenerate response returning a tool call, verifies the follow-up request contains the tool result, and returns final text.
  </action>
  <verify>
    <automated>cargo test --test integration_tests tools</automated>
    <automated>cargo test --lib client</automated>
  </verify>
  <done>
    - generate_with_tools round-trip tested with mocks.
    - Recursion cap terminates repeated tool calls.
    - Unknown tool names produce ToolError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> SDK | Tool list is caller-controlled; SDK must only invoke registered tools. |
| model -> SDK | Tool-call payloads from model are untrusted structured data. |
| SDK -> tool handler | Arguments passed to Tool::invoke may contain unexpected shapes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | Tool call parsing | high | mitigate | Parse defensively; unknown shapes fall back to text; unknown tool names error out. |
| T-05-05 | Denial of Service | generate_with_tools | high | mitigate | Cap tool-call turns at 5 by default; make cap configurable. |
| T-05-03 | Tampering | Tool arguments | medium | mitigate | Each Tool::invoke validates args against its schema; SDK does not coerce. |
</threat_model>

<verification>
  <automated>cargo test --all-targets</automated>
  <automated>cargo clippy --all-targets -- -D warnings</automated>
  <automated>cargo doc --no-deps</automated>
</verification>

<success_criteria>
  - ChatBuilder::with_tools and GenerationConfig::with_tools are public and wired.
  - Tool declarations encode into the 97-slot request list without regression.
  - Parser extracts ToolCall parts and falls back safely.
  - generate_with_tools invokes registered tools and sends a follow-up turn.
  - Recursion is capped and unknown tools produce ToolError.
  - Existing tests continue to pass.
</success_criteria>

<output>
Create `.planning/phases/05-tools-auto-refresh/05-02-SUMMARY.md` when done.
</output>
