---
phase: 06-v1-0-release
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - CHANGELOG.md
  - README.md
  - CONTRIBUTING.md
  - docs/migration-v0-to-v1.md
  - examples/*.rs
autonomous: true
requirements:
  - TOOL-05
must_haves:
  truths:
    - CHANGELOG.md exists at repo root and follows keepachangelog.com format with a v0.1.0 section.
    - README.md accurately lists all shipped features: hooks, tracing, injectable client, upload progress, audio/video, tools, metrics, session save/restore.
    - CONTRIBUTING.md documents the MSRV policy (`rust-version = "1.80"`) and required checks.
    - A migration guide documents breaking changes: async config builders and typed `Error::AttestationFailed`.
    - Exact `cargo publish --all-features` command is documented for the user to run later.
  artifacts:
    - path: CHANGELOG.md
      provides: v0.1.0 release notes
    - path: README.md
      provides: Updated public documentation
    - path: CONTRIBUTING.md
      provides: Contributor guidelines and MSRV policy
    - path: docs/migration-v0-to-v1.md
      provides: Breaking-change migration guide
  key_links:
    - from: CHANGELOG.md
      to: docs/migration-v0-to-v1.md
      via: CHANGELOG links to migration guide
    - from: README.md
      to: docs/migration-v0-to-v1.md
      via: README links to migration guide
    - from: CONTRIBUTING.md
      to: Cargo.toml
      via: MSRV matches rust-version field
---

<objective>
Polish all user-facing documentation for the v0.1.0 release. Produce a CHANGELOG.md, update README.md with the full feature set, document the MSRV policy in CONTRIBUTING.md, and add a migration guide section for v0.x → v1.0 breaking changes.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-v1-0-release/06-CONTEXT.md
@.planning/phases/06-v1-0-release/06-RESEARCH.md
@.planning/phases/06-v1-0-release/06-PATTERNS.md
@README.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md for v0.1.0</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/01-stabilize-v0-1-core/01-0*-SUMMARY.md
    - .planning/phases/02-reliability-protocol-hardening/02-0*-SUMMARY.md
    - .planning/phases/03-observability-configurability/03-0*-SUMMARY.md
    - .planning/phases/04-advanced-media-sessions/04-0*-SUMMARY.md
    - .planning/phases/05-tools-auto-refresh/05-0*-SUMMARY.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create CHANGELOG.md at repo root.
    Follow keepachangelog.com format: Unreleased section plus v0.1.0 section.
    v0.1.0 section summarizes Added/Changed/Deprecated/Removed/Fixed/Security highlights from each completed phase.
    Include note that v1.0.0 bump is planned for a future milestone.
    Link to docs/migration-v0-to-v1.md.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "v0.1.0" CHANGELOG.md && echo "ok"</automated>
  </verify>
  <done>
    - CHANGELOG.md exists and mentions v0.1.0 and migration guide.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with complete feature list</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - src/lib.rs
    - examples/*.rs
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add or expand feature bullets: hooks (`HttpHook`), `tracing`, injectable `reqwest::Client`, upload progress, audio/video uploads, tools/function calling, metrics (`MetricsRecorder` / `metrics` feature), session save/restore (`Snapshot`).
    Update quick-start example if any builder API changed (e.g., async `from_cookie_header`).
    Ensure all README code blocks compile via `cargo test --doc`.
    Add MSRV note and required Rust version.
  </action>
  <verify>
    <automated>cargo test --doc</automated>
    <automated>cargo doc --no-deps</automated>
  </verify>
  <done>
    - README code blocks pass `cargo test --doc`.
    - Feature list is complete and accurate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document MSRV and contribution checks</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (if exists)
    - Cargo.toml
    - .planning/phases/06-v1-0-release/06-CONTEXT.md
  </read_first>
  <action>
    If CONTRIBUTING.md does not exist, create it.
    Document MSRV: `rust-version = "1.80"` in Cargo.toml; MSRV is only raised in minor 0.x or major releases.
    Document required pre-commit checks: `cargo test --all-targets --all-features`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo doc --no-deps --all-features`.
    Document `cargo publish --dry-run --all-features` as the publish-readiness check.
  </action>
  <verify>
    <automated>grep -q "MSRV" CONTRIBUTING.md && grep -q "1.80" CONTRIBUTING.md && grep -q "cargo publish --dry-run" CONTRIBUTING.md && echo "ok"</automated>
  </verify>
  <done>
    - CONTRIBUTING.md contains MSRV, required commands, and dry-run check.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create migration guide for v0.x to v1.0</name>
  <files>docs/migration-v0-to-v1.md</files>
  <read_first>
    - README.md
    - src/client.rs
    - src/errors.rs
    - src/chat.rs
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `docs/migration-v0-to-v1.md`.
    Document async builder methods: `GeminiClient::from_cookie_header` is now async; `with_provider`, `with_metrics`, `with_http_client` are async.
    Document `Error::AttestationFailed` now surfaces as a typed error instead of silently falling back to synthetic context.
    Provide before/after code snippets for each breaking change.
    Note that v1.0.0 version bump itself is deferred to a future milestone.
  </action>
  <verify>
    <automated>test -f docs/migration-v0-to-v1.md && grep -q "AttestationFailed" docs/migration-v0-to-v1.md && grep -q "async" docs/migration-v0-to-v1.md && echo "ok"</automated>
  </verify>
  <done>
    - Migration guide exists and covers async builders and attestation error.
  </done>
</task>

<task type="auto">
  <name>Task 5: Document real cargo publish command</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - README.md
    - .planning/phases/06-v1-0-release/06-CONTEXT.md
  </read_first>
  <action>
    Add a "Release checklist" section in CHANGELOG.md (or create RELEASE.md) listing the exact command the user should run later:
    `cargo publish --all-features`
    Include prerequisite: `cargo login` with crates.io token.
    Include verification steps: tag release, push to GitHub, verify docs on docs.rs.
    Do NOT run the real publish.
  </action>
  <verify>
    <automated>grep -q "cargo publish --all-features" CHANGELOG.md && echo "ok"</automated>
  </verify>
  <done>
    - Release checklist with exact publish command is documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> users | README examples and migration guide must be accurate and compile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | README examples drift | low | mitigate | Run `cargo test --doc` to verify README code blocks. |
| T-06-05 | Denial of Service | Migration guide omission | low | mitigate | Explicitly list async builders and attestation error breaking changes. |
</threat_model>

<verification>
  <automated>cargo test --all-features --all-targets</automated>
  <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
  <automated>cargo doc --no-deps --all-features</automated>
  <automated>cargo test --doc</automated>
</verification>

<success_criteria>
  - CHANGELOG.md exists and is valid markdown.
  - README.md reflects all shipped features and passes `cargo test --doc`.
  - CONTRIBUTING.md documents MSRV and required checks.
  - docs/migration-v0-to-v1.md exists and covers async builders + attestation error change.
  - Exact `cargo publish --all-features` command is documented for user action.
  - All tooling gates still pass after documentation changes.
</success_criteria>

<output>
Create `.planning/phases/06-v1-0-release/06-02-SUMMARY.md` when done.
</output>

PLANNING COMPLETE
