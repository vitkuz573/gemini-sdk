---
phase: 06-v1-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/*.rs
autonomous: true
requirements:
  - TOOL-05
  - API-04
  - TOOL-02
  - TOOL-03
  - TOOL-01
must_haves:
  truths:
    - `cargo test --all-features --all-targets` passes.
    - `cargo clippy --all-targets --all-features -- -D warnings` passes.
    - `cargo doc --no-deps --all-features` builds with zero warnings under `#![deny(missing_docs)]`.
    - `cargo publish --dry-run --all-features` succeeds.
    - Public API review confirms no raw WIZ slot constants, HTML extraction internals, or attestation protocol leaks in `src/lib.rs` re-exports.
    - Leftover `#[allow(unused)]` and dead-code suppressions are either justified or removed.
  artifacts:
    - path: Cargo.toml
      provides: Validated publish-ready manifest
    - path: src/lib.rs
      provides: Clean public API surface with no unintended leaks
  key_links:
    - from: src/lib.rs
      to: Cargo.toml
      via: Public re-exports align with published crate metadata
---

<objective>
Run the final pre-publish tooling gates and public-API audit for the v0.1.0 release, delivering TOOL-05 readiness. This plan ensures the crate passes `cargo test`, `cargo clippy`, `cargo doc`, and `cargo publish --dry-run`, and that the public API surface does not leak unintended protocol internals.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-v1-0-release/06-CONTEXT.md
@.planning/phases/06-v1-0-release/06-RESEARCH.md
@.planning/phases/06-v1-0-release/06-PATTERNS.md
@src/lib.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Cargo.toml publish-required fields</name>
  <files>Cargo.toml, LICENSE</files>
  <read_first>
    - Cargo.toml
    - LICENSE
  </read_first>
  <action>
    Confirm all required fields present: name, version, authors, edition, license, description, repository, readme, keywords, categories, rust-version.
    Confirm version remains `0.1.0` per CONTEXT.md Version Policy.
    Confirm `license = "MIT"` matches LICENSE file content.
    Confirm `rust-version = "1.80"`.
    If any field is missing or inconsistent, edit Cargo.toml to fix it.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 | jq -e '.packages[0] | .name == "gemini-sdk" and .version == "0.1.0" and .license == "MIT"'</automated>
  </verify>
  <done>
    - Cargo.toml contains all required publish fields.
    - Version remains 0.1.0 and license matches LICENSE.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit src/lib.rs public re-exports</name>
  <files>src/lib.rs</files>
  <read_first>
    - src/lib.rs
    - src/proto/mod.rs
    - src/session.rs
    - src/retry.rs
  </read_first>
  <action>
    Confirm `src/lib.rs` re-exports only intended public types.
    Remove or make `pub(crate)` any re-export that exposes WIZ slot constants, raw HTML extraction helpers, retry internals, or session state internals.
    Ensure every public re-export has a doc comment or is covered by `#![deny(missing_docs)]`.
    Ensure no new `pub` items were added by earlier phases without re-export or documentation.
  </action>
  <verify>
    <automated>cargo doc --no-deps --all-features</automated>
    <automated>cargo install cargo-public-api --locked 2>/dev/null || true; cargo public-api --target-version 0.1.0 2>/dev/null || echo "manual review"</automated>
  </verify>
  <done>
    - `cargo doc` builds with zero warnings.
    - Public API audit documented (tool output or manual review notes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up dead code and unnecessary lints</name>
  <files>src/lib.rs, src/*.rs</files>
  <read_first>
    - src/lib.rs (lint suppressions)
    - All src/*.rs files for leftover #[allow(unused)] or unused imports
  </read_first>
  <action>
    Remove `#[allow(unused)]` or `dead_code` suppressions that are no longer needed.
    If a suppression is still needed, add a short `// SAFETY:` or `// REASON:` comment explaining why.
    Do not change functional behavior.
  </action>
  <verify>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --all-features --all-targets</automated>
  </verify>
  <done>
    - Clippy passes with no warnings.
    - All tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run cargo publish --dry-run</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml
    - .gitignore
  </read_first>
  <action>
    Run `cargo publish --dry-run --all-features`.
    Capture the full output.
    Fix any packaging or dependency errors it reports.
    Do NOT run a real publish.
  </action>
  <verify>
    <automated>cargo publish --dry-run --all-features</automated>
  </verify>
  <done>
    - Dry-run exits 0 with no packaging warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source -> crate tarball | `cargo publish` packages only intended files; internal planning/docs excluded. |
| SDK -> consumers | Public API exposes only intended types; no protocol internals leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | crate tarball | medium | mitigate | Confirm `.planning/`, `.opencode/`, `docs/` in `exclude`; review dry-run output. |
| T-06-02 | Information Disclosure | public API | medium | mitigate | Audit `src/lib.rs` re-exports; remove unintended protocol internals. |
| T-06-03 | Tampering | license mismatch | low | mitigate | Verify `license = "MIT"` matches LICENSE file. |
</threat_model>

<verification>
  <automated>cargo test --all-features --all-targets</automated>
  <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
  <automated>cargo doc --no-deps --all-features</automated>
  <automated>cargo publish --dry-run --all-features</automated>
</verification>

<success_criteria>
  - `cargo test --all-features --all-targets` passes.
  - `cargo clippy --all-targets --all-features -- -D warnings` passes.
  - `cargo doc --no-deps --all-features` builds with zero warnings.
  - `cargo publish --dry-run --all-features` exits 0.
  - Public API audit confirms no unintended protocol internals exposed.
  - Dead-code/unused suppressions cleaned or justified.
</success_criteria>

<output>
Create `.planning/phases/06-v1-0-release/06-01-SUMMARY.md` when done.
</output>

PLANNING COMPLETE
