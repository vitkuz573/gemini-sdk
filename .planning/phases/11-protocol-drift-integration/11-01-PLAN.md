---
phase: 11-protocol-drift-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client.rs
  - examples/v0_2_api_tour.rs
  - Cargo.toml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DRIFT-01
  - TOOL-06
  - TOOL-07
must_haves:
  truths:
    - "The X_CLIENT_DATA constant in src/client.rs equals CNeOywE=."
    - "Every v0.2 RPC (PCck7e, o30O0e, L5adhe, cYRIkd, whPPme, Te6DCf, ku4Jyf, jSf9Qc, XPSWpd) has a mocked fixture integration test."
    - "A new example binary v0_2_api_tour exists, is declared in Cargo.toml, and compiles with cargo build --examples."
    - "cargo test --all-targets passes."
    - "cargo clippy --all-targets -- -D warnings passes."
    - "cargo doc --no-deps passes."
  artifacts:
    - src/client.rs
    - examples/v0_2_api_tour.rs
    - Cargo.toml
  key_links:
    - "X_CLIENT_DATA constant -> all build_headers/waa_create/ogads call sites."
    - "Cargo.toml [[example]] declaration -> examples/v0_2_api_tour.rs compilation."
---

<objective>
Apply the v0.2 protocol drift fix, add a runnable example that showcases the new v0.2 APIs, confirm mocked fixture coverage for every new RPC, and make the final quality gates green.

Purpose: This is the integration and final-verification phase for the v0.2 API Expansion milestone.
Output: Updated constant, new example, verified fixture coverage, green CI gates, and updated planning state.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-protocol-drift-integration/11-CONTEXT.md
@.planning/phases/11-protocol-drift-integration/11-RESEARCH.md
@.planning/phases/07-conversation-actions/07-01-SUMMARY.md
@.planning/phases/08-user-profile-preferences/08-01-SUMMARY.md
@.planning/phases/09-locale-model-config/09-01-SUMMARY.md
@.planning/phases/10-settings-pages/10-01-SUMMARY.md
@.opencode/skills/spike-findings-gemini-sdk/sources/001-har-api-coverage/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update x-client-data constant to match latest HAR</name>
  <files>src/client.rs</files>
  <action>Replace the X_CLIENT_DATA constant value from "CI7yygE=" to "CNeOywE=" in src/client.rs line 106. Do not rename the constant or change any call sites; the constant is already used by build_headers, waa_create, and ogads_get_async_data. Verify the change with a project-wide grep for the old value (it should only remain in historical planning/spike docs).</action>
  <verify>
    <automated>grep -n 'X_CLIENT_DATA: &str' src/client.rs && grep -v '^#' src/client.rs | grep -c 'CI7yygE=' | grep -q '^0$'</automated>
  </verify>
  <done>X_CLIENT_DATA equals "CNeOywE=" and no stale value remains in src/client.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Add v0.2 API tour example</name>
  <files>examples/v0_2_api_tour.rs, Cargo.toml</files>
  <action>Create examples/v0_2_api_tour.rs that initializes a GeminiClient from GEMINI_COOKIES, then calls get_user_info, get_last_selected_mode, get_locale_tools, get_model_config, and get_usage_stats, printing concise results. Use tracing_subscriber::fmt::init() for observability. Return gemini_sdk::Result<()>. Then add a matching [[example]] entry in Cargo.toml with path = "examples/v0_2_api_tour.rs" and no required-features. Do not include conversation actions or set_last_selected_mode to keep the example read-only and safe against live cookies. Follow the existing example style in examples/text_chat.rs.</action>
  <verify>
    <automated>cargo build --examples</automated>
  </verify>
  <done>The example file exists, is declared in Cargo.toml, and cargo build --examples succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Audit fixture coverage and run final quality gates</name>
  <files>tests/integration_tests.rs, tests/fixtures/*</files>
  <action>Verify that each v0.2 RPC (PCck7e, o30O0e, L5adhe, cYRIkd, whPPme, Te6DCf, ku4Jyf, jSf9Qc, XPSWpd) has at least one corresponding wiremock integration test and fixture file. If any are missing, add the missing fixture and test before proceeding. Then run the full quality gate sequence: cargo test --all-targets, cargo clippy --all-targets -- -D warnings, and cargo doc --no-deps. Fix any failures that surface.</action>
  <verify>
    <automated>cargo test --all-targets && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps</automated>
  </verify>
  <done>All nine RPCs have fixture tests, all tests pass, clippy is clean, and docs build without warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> Gemini frontend | x-client-data is an opaque telemetry header; updating it to match current browser traffic reduces fingerprint divergence. |
| Example -> user env | Example reads GEMINI_COOKIES and sends it to live Gemini; it must not log or print cookie values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | examples/v0_2_api_tour.rs | medium | mitigate | Do not print the cookie header or raw credentials; only print API response summaries. |
| T-11-02 | Tampering | src/client.rs X_CLIENT_DATA | low | mitigate | Update the constant to the verified HAR value "CNeOywE=" and confirm no stale references remain. |
</threat_model>

<verification>
- `grep -n 'X_CLIENT_DATA' src/client.rs` shows the value "CNeOywE=".
- `cargo test --all-targets` passes with 2 ignored live-cookie tests.
- `cargo clippy --all-targets -- -D warnings` is clean.
- `cargo doc --no-deps` builds with no warnings.
- `cargo build --examples` compiles all examples including v0_2_api_tour.
- REQUIREMENTS.md and ROADMAP.md are updated to mark DRIFT-01, TOOL-06, and TOOL-07 complete.
</verification>

<success_criteria>
- DRIFT-01 satisfied: X_CLIENT_DATA is "CNeOywE=".
- TOOL-06 satisfied: every v0.2 RPC has a mocked fixture test.
- TOOL-07 satisfied: cargo test, cargo clippy, and cargo doc all pass.
- A new runnable example demonstrates v0.2 APIs and compiles cleanly.
- Planning state is updated to reflect Phase 11 completion.
</success_criteria>

<output>
Create `.planning/phases/11-protocol-drift-integration/11-01-SUMMARY.md` when done.
</output>
