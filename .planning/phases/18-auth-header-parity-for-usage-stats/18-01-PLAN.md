---
phase: 18-auth-header-parity-for-usage-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants.rs
  - src/client.rs
autonomous: true
requirements:
  - AUTH-03
  - REQ-02
  - TEST-04
must_haves:
  truths:
    - `src/constants.rs` exposes a `pub(crate)` `X_GOOG_AUTHUSER` header name constant equal to `"x-goog-authuser"` and a `pub(crate)` `X_GOOG_AUTHUSER_VALUE` constant equal to `"0"`.
    - `GeminiClient::build_headers` accepts an optional `auth_user: Option<&str>` parameter (or equivalent) and emits `x-goog-authuser` when it is provided alongside `authorization`.
    - No existing batchexecute RPC method emits `Authorization` or `x-goog-authuser` after this plumbing change unless it explicitly passes them.
    - `cargo check` passes with no new warnings.
  artifacts:
    - src/constants.rs
    - src/client.rs
  key_links:
    - `src/constants.rs` header constants ↔ `src/client.rs` `build_headers`
---

<objective>
Add the `x-goog-authuser` constants and extend `GeminiClient::build_headers` so it can emit both `Authorization` and `x-goog-authuser` when an RPC opts in. This plan produces the plumbing required by Plan 02.

Purpose: Centralize the new header literals and provide an opt-in mechanism so auth headers are scoped to `jSf9Qc` only.
Output: Updated `src/constants.rs` and `src/client.rs` with no behavior change yet.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-CONTEXT.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-RESEARCH.md
@src/constants.rs
@src/client.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add x-goog-authuser constants to src/constants.rs</name>
  <files>src/constants.rs</files>
  <read_first>
    - src/constants.rs
    - .planning/phases/18-auth-header-parity-for-usage-stats/18-CONTEXT.md
  </read_first>
  <action>
    In `src/constants.rs`, inside the `pub mod headers` block, add two new `pub(crate)` constants:
    - `X_GOOG_AUTHUSER` with value `"x-goog-authuser"` (header name).
    - `X_GOOG_AUTHUSER_VALUE` with value `"0"` (observed browser value).
    Place them near the other `x-goog-*` header constants. Do not change visibility of existing constants. Add a short doc comment to each citing HAR/browser observation.
  </action>
  <verify>
    <automated>grep -n "X_GOOG_AUTHUSER" src/constants.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src/constants.rs` contains `pub(crate) const X_GOOG_AUTHUSER: &str = "x-goog-authuser";`.
    - `src/constants.rs` contains `pub(crate) const X_GOOG_AUTHUSER_VALUE: &str = "0";`.
    - Both constants are inside `pub mod headers`.
    - `cargo check` exits 0.
  </acceptance_criteria>
  <done>`X_GOOG_AUTHUSER` and `X_GOOG_AUTHUSER_VALUE` constants exist in `src/constants.rs` and the crate checks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend build_headers to emit Authorization + x-goog-authuser when opted in</name>
  <files>src/client.rs</files>
  <read_first>
    - src/client.rs (especially `build_headers` around line 2639 and all callers)
    - src/constants.rs
  </read_first>
  <action>
    Change the signature of `GeminiClient::build_headers` from:
    `async fn build_headers(&self, reqid: Option<&str>, waa_context: Option<&str>, authorization: Option<&str>, endpoint: Option<&str>) -> Vec<(String, String)>`
    to:
    `async fn build_headers(&self, reqid: Option<&str>, waa_context: Option<&str>, authorization: Option<&str>, auth_user: Option<&str>, endpoint: Option<&str>) -> Vec<(String, String)>`.
    Inside `build_headers`, after the existing `if let Some(auth) = authorization` block that adds `header_constants::AUTHORIZATION`, add a new block:
    `if let Some(user) = auth_user { headers.push((header_constants::X_GOOG_AUTHUSER.to_string(), user.to_string())); }`.
    Update every call site of `build_headers` in `src/client.rs` to pass `None` for the new `auth_user` parameter, preserving the existing `authorization` and `endpoint` arguments. Callers include `regenerate_turn`, `rate_turn`, `delete_turn`, `get_user_info`, `get_last_selected_mode`, `set_last_selected_mode`, `get_locale_tools`, `get_model_config`, `get_locale_config`, `get_tools_config`, `get_usage_stats`, `get_scheduled_prompts`, and any other internal usage.
  </action>
  <verify>
    <automated>cargo check 2>&1 | tail -n 10</automated>
  </verify>
  <acceptance_criteria>
    - `build_headers` signature includes `auth_user: Option<&str>` as the fourth positional parameter (before `endpoint`).
    - `build_headers` pushes `x-goog-authuser` header when `auth_user` is `Some`.
    - Every call site in `src/client.rs` compiles with the new signature.
    - `cargo check` exits 0 with no new warnings.
  </acceptance_criteria>
  <done>`build_headers` accepts `auth_user` and emits the header; all callers compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Gemini backend | Headers cross the network; secrets must not leak in logs/HAR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | `Authorization` header in HAR | high | mitigate | Already redacted by `src/har.rs`; verified in Plan 03. |
| T-18-02 | Tampering | `x-goog-authuser` value | low | accept | Constant value `"0"` matches browser; no user input. |
</threat_model>

<verification>
- `cargo check` passes.
- `cargo clippy --all-targets -- -D warnings` passes (no new warnings from signature change).
- `cargo test --all-targets` passes (no behavior change yet).
</verification>

<success_criteria>
- Constants added and used only inside `build_headers`.
- All call sites updated; project compiles.
- No headers are emitted by any RPC yet.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-header-parity-for-usage-stats/18-01-SUMMARY.md`
</output>

## Artifacts this phase produces

- `src/constants.rs`:
  - `pub(crate) const X_GOOG_AUTHUSER: &str`
  - `pub(crate) const X_GOOG_AUTHUSER_VALUE: &str`
- `src/client.rs`:
  - Updated `GeminiClient::build_headers` signature and body
  - Updated all internal call sites to pass `None` for `auth_user`
