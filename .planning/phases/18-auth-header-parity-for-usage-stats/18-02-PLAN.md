---
phase: 18-auth-header-parity-for-usage-stats
plan: 02
type: tdd
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/client.rs
  - tests/integration_tests.rs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - TEST-04
must_haves:
  truths:
    - `GeminiClient::get_usage_stats` computes `Authorization: SAPISIDHASH <ts>_<sha1>` from the active credentials and passes it to `build_headers`.
    - `GeminiClient::get_usage_stats` passes `x-goog-authuser: 0` to `build_headers` via the `auth_user` parameter.
    - A wiremock integration test verifies the outgoing `jSf9Qc` request contains both headers.
    - `cargo test --all-targets` passes.
  artifacts:
    - src/client.rs
    - tests/integration_tests.rs
  key_links:
    - `src/auth.rs` `Credentials::sapisid_hash` ↔ `src/client.rs` `credentials_to_sapisid_hash` helper
    - `src/client.rs` `get_usage_stats` ↔ `build_headers` auth parameters
---

<objective>
Wire SAPISIDHASH + `x-goog-authuser: 0` into `GeminiClient::get_usage_stats` only, using TDD.

Purpose: Make the `jSf9Qc` usage-stats RPC send the same auth headers the browser sends, fixing the empty-response issue.
Output: `get_usage_stats` emits `Authorization` and `x-goog-authuser`, with a wiremock test proving it.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
@/home/vitaly/.config/opencode/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-CONTEXT.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-RESEARCH.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-01-PLAN.md
@src/client.rs
@src/constants.rs
@src/auth.rs
@tests/integration_tests.rs
@tests/common/mod.rs
</context>

<feature>
  <name>get_usage_stats sends Authorization and x-goog-authuser</name>
  <files>src/client.rs, tests/integration_tests.rs</files>
  <behavior>
    - Given a `GeminiClient` built from `MOCK_COOKIE_HEADER` (which includes `__Secure-1PAPISID`), when `get_usage_stats()` is called against a wiremock batchexecute endpoint, the captured request headers include `Authorization: SAPISIDHASH <ts>_<sha1>` and `x-goog-authuser: 0`.
    - The test asserts the `Authorization` value starts with `SAPISIDHASH ` and contains a `_` separator (do not assert the exact timestamp/hash because `sapisid_hash` uses `SystemTime::now()`).
  </behavior>
  <implementation>
    In `src/client.rs::get_usage_stats`:
    1. After obtaining `cookies`, compute `let auth = credentials_to_sapisid_hash(&cookies, &base_url);`.
    2. Change the `build_headers` call to pass `auth.as_deref()` for `authorization` and `Some(crate::constants::headers::X_GOOG_AUTHUSER_VALUE)` for `auth_user`.
    In `tests/integration_tests.rs`:
    1. Add assertions to the existing `get_usage_stats_returns_value` test (or a new test) that inspect `mock_server.received_requests().await` and verify both headers are present.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Add failing integration test for jSf9Qc auth headers</name>
  <files>tests/integration_tests.rs</files>
  <read_first>
    - tests/integration_tests.rs (existing `get_usage_stats_returns_value` test and wiremock helpers)
    - tests/common/mod.rs
    - src/client.rs (current `get_usage_stats` implementation)
  </read_first>
  <behavior>
    - Capture the outgoing `jSf9Qc` request from `get_usage_stats` against a wiremock server.
    - Assert the request contains an `Authorization` header whose value starts with `SAPISIDHASH ` and includes `_`.
    - Assert the request contains an `x-goog-authuser` header with value `0`.
  </behavior>
  <action>
    Add a new async test `get_usage_stats_sends_auth_headers` in `tests/integration_tests.rs`. Set up a wiremock `MockServer` with a `POST` matcher for `BATCHEXECUTE_PATH` returning the existing `jSf9Qc_usage_stats.txt` fixture. Build a `GeminiClient` from `MOCK_COOKIE_HEADER`, inject `build_label`, `session_id`, and `access_token` into the session, and call `get_usage_stats()`. After the call succeeds, retrieve `mock_server.received_requests().await`, find the request, and assert:
    - `headers.get("Authorization")` is `Some` and its value starts with `SAPISIDHASH ` and contains `_`.
    - `headers.get("x-goog-authuser")` is `Some` and equals `0`.
    Run the test; it MUST fail because `get_usage_stats` currently does not pass auth headers.
  </action>
  <verify>
    <automated>cargo test --test integration_tests get_usage_stats_sends_auth_headers 2>&1 | tail -n 20</automated>
  </verify>
  <acceptance_criteria>
    - A new test `get_usage_stats_sends_auth_headers` exists in `tests/integration_tests.rs`.
    - Running the test fails with an assertion about missing `Authorization` or `x-goog-authuser` headers.
    - The test compiles before the implementation change.
  </acceptance_criteria>
  <done>Test exists and fails for the expected reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Wire auth headers into get_usage_stats</name>
  <files>src/client.rs</files>
  <read_first>
    - src/client.rs (`get_usage_stats` around line 1389 and `credentials_to_sapisid_hash` around line 2969)
    - src/constants.rs (`X_GOOG_AUTHUSER_VALUE` added in Plan 18-01)
  </read_first>
  <behavior>
    - `get_usage_stats` emits `Authorization: SAPISIDHASH ...` and `x-goog-authuser: 0`.
  </behavior>
  <action>
    In `src/client.rs::get_usage_stats`:
    1. After `let cookies = self.cookies().await;`, compute `let auth = credentials_to_sapisid_hash(&cookies, &base_url);`.
    2. Change the `build_headers` call from `build_headers(None, None, None, Some(transport::BATCHEXECUTE_ENDPOINT))` to `build_headers(None, None, auth.as_deref(), Some(crate::constants::headers::X_GOOG_AUTHUSER_VALUE), Some(transport::BATCHEXECUTE_ENDPOINT))`.
    Ensure the import path for `X_GOOG_AUTHUSER_VALUE` resolves (it lives in `crate::constants::headers`).
  </action>
  <verify>
    <automated>cargo test --test integration_tests get_usage_stats_sends_auth_headers 2>&1 | tail -n 20</automated>
  </verify>
  <acceptance_criteria>
    - `get_usage_stats` calls `credentials_to_sapisid_hash(&cookies, &base_url)`.
    - `get_usage_stats` passes the computed auth and `X_GOOG_AUTHUSER_VALUE` into `build_headers`.
    - The new integration test passes.
  </acceptance_criteria>
  <done>`get_usage_stats` sends both auth headers and the new test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — Clean up imports and helper visibility if needed</name>
  <files>src/client.rs</files>
  <read_first>
    - src/client.rs (imports and helper location)
  </read_first>
  <behavior>
    - No behavior change; only import/module hygiene.
  </behavior>
  <action>
    Review the imports in `src/client.rs`. If `X_GOOG_AUTHUSER_VALUE` is referenced via a long path, consider importing it alongside existing `header_constants` usage. Ensure `credentials_to_sapisid_hash` remains a private `fn` (no visibility change needed). Run `cargo clippy --all-targets -- -D warnings` and fix any new warnings.
  </action>
  <verify>
    <automated>cargo test --test integration_tests get_usage_stats_sends_auth_headers && cargo clippy --all-targets -- -D warnings 2>&1 | tail -n 10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - The integration test still passes.
    - No new `allow` attributes introduced without justification.
  </acceptance_criteria>
  <done>Code is clean and clippy-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Gemini backend | Auth headers cross the network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Information Disclosure | `Authorization` header logged by SDK | high | mitigate | Never log `Authorization` at info level; HAR redaction covers it (Plan 03). |
</threat_model>

<verification>
- `cargo test --test integration_tests get_usage_stats_sends_auth_headers` passes.
- `cargo test --all-targets` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
</verification>

<success_criteria>
- RED test committed and failed.
- Implementation committed and made the test pass.
- Refactor committed if any cleanup was needed.
- `get_usage_stats` and only `get_usage_stats` sends the two auth headers.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-header-parity-for-usage-stats/18-02-SUMMARY.md`
</output>

## Artifacts this phase produces

- `src/client.rs`:
  - `get_usage_stats` now passes `auth.as_deref()` and `X_GOOG_AUTHUSER_VALUE` to `build_headers`
- `tests/integration_tests.rs`:
  - New async test `get_usage_stats_sends_auth_headers`
