---
phase: 18-auth-header-parity-for-usage-stats
plan: 03
type: tdd
wave: 2
depends_on:
  - 18-01
files_modified:
  - tests/integration_tests.rs
  - src/har.rs
autonomous: true
requirements:
  - AUTH-03
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - Other batchexecute RPCs (`get_user_info`, `list_models`, `get_scheduled_prompts`, `get_locale_config`, `get_model_config`, `get_tools_config`, `get_locale_tools`, `set_last_selected_mode`, `get_last_selected_mode`, conversation actions) do not send `Authorization` or `x-goog-authuser` headers.
    - HAR redaction masks the `Authorization` header value.
    - All quality gates pass.
  artifacts:
    - tests/integration_tests.rs
    - src/har.rs
  key_links:
    - `src/har.rs` `is_secret_header` ↔ HAR redaction tests
    - Integration tests ↔ `build_headers` auth scoping
---

<objective>
Verify auth header isolation (only `jSf9Qc` gets them) and HAR redaction of `Authorization`, using TDD.

Purpose: Ensure the auth fix does not leak to unrelated RPCs and that captured HAR files do not contain recoverable auth tokens.
Output: Isolation tests for other RPCs and a HAR redaction unit test for `Authorization`.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
@/home/vitaly/.config/opencode/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-CONTEXT.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-RESEARCH.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-01-PLAN.md
@.planning/phases/18-auth-header-parity-for-usage-stats/18-02-PLAN.md
@src/har.rs
@src/client.rs
@tests/integration_tests.rs
</context>

<feature>
  <name>Auth headers isolated to jSf9Qc and redacted in HAR</name>
  <files>tests/integration_tests.rs, src/har.rs</files>
  <behavior>
    - For a non-`jSf9Qc` RPC (e.g., `get_user_info`), the outgoing wiremock request must not contain `Authorization` or `x-goog-authuser` headers.
    - HAR redaction must replace the `Authorization` header value with `<redacted>`.
  </behavior>
  <implementation>
    Add an isolation test in `tests/integration_tests.rs` that calls `get_user_info` (or another existing RPC test) and asserts the captured request lacks both auth headers. Add a unit test in `src/har.rs` that builds a `HeaderMap` containing `Authorization: SAPISIDHASH 123_abc`, runs `redact_headers`, and asserts the returned value is `<redacted>`.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Add failing isolation test for get_user_info auth headers</name>
  <files>tests/integration_tests.rs</files>
  <read_first>
    - tests/integration_tests.rs (existing `get_user_info_returns_profile` or similar RPC test)
    - src/client.rs (call sites updated in Plan 18-01)
  </read_first>
  <behavior>
    - `get_user_info` against a wiremock server must not include `Authorization` or `x-goog-authuser` headers.
  </behavior>
  <action>
    Add a new async test `get_user_info_does_not_send_auth_headers` in `tests/integration_tests.rs`. Set up a wiremock `MockServer` with a `POST` matcher for `BATCHEXECUTE_PATH` returning the existing user-info fixture (or an empty success body). Build a client from `MOCK_COOKIE_HEADER`, inject session state, and call `get_user_info()`. After the call, inspect the first received request's headers and assert:
    - `headers.get("Authorization")` is `None`.
    - `headers.get("x-goog-authuser")` is `None`.
    Run the test; it should pass immediately because Plan 18-01 did not wire auth to other RPCs. If it passes, this is acceptable RED because the test documents the invariant; still commit it as the RED test.
  </action>
  <verify>
    <automated>cargo test --test integration_tests get_user_info_does_not_send_auth_headers 2>&1 | tail -n 20</automated>
  </verify>
  <acceptance_criteria>
    - Test `get_user_info_does_not_send_auth_headers` exists and compiles.
    - Running it shows the expected isolation behavior (pass is acceptable if auth is already absent; fail would indicate a leak).
    - The test uses `MOCK_COOKIE_HEADER` which contains a SAPISID source, so a missing SAPISID cannot explain the absence of headers.
  </acceptance_criteria>
  <done>Isolation test exists and passes (or fails for expected leak).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Add HAR redaction test for Authorization</name>
  <files>src/har.rs</files>
  <read_first>
    - src/har.rs (`is_secret_header` and inline tests)
    - src/constants.rs (HAR redaction value constant)
  </read_first>
  <behavior>
    - `redact_headers` replaces the value of an `Authorization` header with `<redacted>`.
  </behavior>
  <action>
    In `src/har.rs` inline test module, add a test `authorization_header_is_redacted` that:
    1. Creates a `HeaderMap`.
    2. Inserts `Authorization` with value `SAPISIDHASH 123_abc`.
    3. Calls `redact_headers(&map)`.
    4. Finds the entry with name `authorization` and asserts its `"value"` field equals `<redacted>`.
    Run the test; it should pass because `is_secret_header` already treats `Authorization` as secret.
  </action>
  <verify>
    <automated>cargo test authorization_header_is_redacted 2>&1 | tail -n 20</automated>
  </verify>
  <acceptance_criteria>
    - New test `authorization_header_is_redacted` exists in `src/har.rs` inline tests.
    - The test asserts the redacted value is `<redacted>`.
    - The test passes.
  </acceptance_criteria>
  <done>HAR redaction test for Authorization passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — Generalize isolation test helper if duplication exists</name>
  <files>tests/integration_tests.rs</files>
  <read_first>
    - tests/integration_tests.rs
  </read_first>
  <behavior>
    - No behavior change; reduce test duplication only if it improves readability.
  </behavior>
  <action>
    If multiple isolation tests are added and they share setup boilerplate, extract a small private helper (e.g., `async fn inject_test_session(client: &GeminiClient)`) in `tests/integration_tests.rs`. Do not introduce helper abstractions for a single test. Run `cargo clippy --all-targets -- -D warnings` and fix any new warnings.
  </action>
  <verify>
    <automated>cargo test --all-targets 2>&1 | tail -n 10 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -n 10</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass.
    - Clippy passes.
    - No unnecessary helper added for a single test.
  </acceptance_criteria>
  <done>Tests and code are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → disk (HAR) | `Authorization` value must be redacted before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Information Disclosure | `Authorization` in HAR file | high | mitigate | `is_secret_header` flags `Authorization`; new unit test verifies redaction. |
| T-18-05 | Elevation of Privilege | Auth headers on wrong RPC | medium | mitigate | Isolation test asserts absence on `get_user_info`. |
</threat_model>

<verification>
- `cargo test --test integration_tests get_user_info_does_not_send_auth_headers` passes.
- `cargo test authorization_header_is_redacted` passes.
- `cargo test --all-targets` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
</verification>

<success_criteria>
- Isolation test proves other RPCs do not receive auth headers.
- HAR redaction unit test proves `Authorization` is masked.
- All quality gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-header-parity-for-usage-stats/18-03-SUMMARY.md`
</output>

## Artifacts this phase produces

- `tests/integration_tests.rs`:
  - New async test `get_user_info_does_not_send_auth_headers`
- `src/har.rs`:
  - New unit test `authorization_header_is_redacted`
