---
phase: 01-stabilize-v0-1-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/auth.rs
  - src/client.rs
  - src/lib.rs
  - tests/auth_provider.rs
  - tests/redaction.rs
  - Cargo.toml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - Cookie header parsing rejects missing required cookies.
    - A CredentialsProvider trait exists and a default header-string provider works.
    - GeminiClient can be constructed from a provider without changing the existing cookie-header constructor.
    - Credentials Debug output contains no secret material.
  artifacts:
    - src/auth.rs with CredentialsProvider trait and CookieHeaderProvider impl.
    - src/auth.rs with fully-redacted Debug for Credentials.
    - src/client.rs with async from_provider constructor.
    - src/lib.rs re-exports provider types.
    - tests/auth_provider.rs covering provider trait.
    - tests/redaction.rs integration test verifying no secret material in Debug.
  key_links:
    - CredentialsProvider -> Credentials -> GeminiClient::from_provider.
    - Debug impl -> redaction test -> no secret substring appears in output.
---

<objective>
Improve auth ergonomics by introducing a pluggable `CredentialsProvider` trait and fully redacting credentials in `Debug` output.

Purpose: Let advanced users source credentials from env/file/keyring without changing the simple cookie-header constructor, and eliminate secret leakage in logs.
Output: Trait-based auth layer with full redaction and tests.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-stabilize-v0-1-core/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@src/auth.rs
@src/client.rs
@src/lib.rs
@Cargo.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement full credential redaction with integration test</name>
  <files>src/auth.rs, tests/redaction.rs</files>
  <behavior>
    - Test 1: format!("{:?}", creds) contains no substring of any secret value.
    - Test 2: Empty secrets render as "(empty)" and non-empty secrets render as "<redacted>".
    - Test 3: Existing cookie round-trip and parsing tests still pass.
  </behavior>
  <action>
    First create `tests/redaction.rs` as a Wave 0 integration test. Construct a `Credentials` instance with synthetic secrets for each field, assert `format!("{:?}", creds)` contains "<redacted>" for non-empty values and "(empty)" for empty values, and assert that none of the secret substrings appear in the output. The test may initially fail (TDD) if the current Debug does not fully redact. Then replace the partial redaction in `Credentials` Debug with a helper that returns "<redacted>" for any non-empty secret and "(empty)" for empty strings. Apply it to psid, psidcc, psidts, papisid, sapisid, apisid, and socs. Continue to show only the count of extra cookies. Update the existing `credentials_debug_redacts_secrets` test in src/auth.rs to assert no secret substrings appear and that the output contains "<redacted>".
  </action>
  <verify>
    <automated>cargo test --lib auth --quiet && cargo test --test redaction --quiet</automated>
  </verify>
  <done>All secrets are fully redacted in Debug output and integration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce CredentialsProvider trait and default provider</name>
  <files>src/auth.rs, src/client.rs, src/lib.rs, tests/auth_provider.rs</files>
  <behavior>
    - Test 1: A custom struct implements CredentialsProvider and yields a Credentials instance.
    - Test 2: CookieHeaderProvider parses a valid header and returns Credentials.
    - Test 3: CookieHeaderProvider rejects a header missing PSIDCC.
    - Test 4: GeminiClient::from_provider builds a client from a boxed provider.
  </behavior>
  <action>
    Define `pub trait CredentialsProvider: Send + Sync` in src/auth.rs with an async method returning `Pin<Box<dyn Future<Output = crate::Result<Credentials>> + Send + '_>>` (matches RESEARCH.md Pattern 4). Provide a blanket impl for `Credentials` so a bare `Credentials` value satisfies the trait. Provide a `CookieHeaderProvider` struct that wraps a header string and parses it on demand. Add `pub async fn from_provider<P: CredentialsProvider + 'static>(provider: P) -> crate::Result<Self>` in src/client.rs that awaits the provider's credentials and delegates to `from_credentials`. Re-export `CredentialsProvider` and `CookieHeaderProvider` from src/lib.rs. In tests/auth_provider.rs, write integration tests for the trait, default provider, and constructor. Keep existing constructors unchanged.
  </action>
  <verify>
    <automated>cargo test --test auth_provider --quiet && cargo test --lib --quiet</automated>
  </verify>
  <done>CredentialsProvider trait and default provider are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire public exports and verify no unapproved dependencies</name>
  <files>src/lib.rs, Cargo.toml</files>
  <behavior>
    - Test 1: `use gemini_sdk::{CredentialsProvider, CookieHeaderProvider};` compiles from a downstream perspective.
    - Test 2: No new runtime dependencies are added; any new crate is dev-only.
  </behavior>
  <action>
    Verify Cargo.toml only adds `static_assertions` (already planned in 01-01) as a dev-dependency and no runtime crate for this plan. If additional crates are needed for the provider trait, document them and insert a blocking `checkpoint:human-verify` before any `cargo add` of an [ASSUMED]/[SUS] package. Update src/lib.rs re-exports so the provider types appear at the crate root. Confirm `cargo check`, `cargo clippy --all-targets -- -D warnings`, and `cargo doc --no-deps` produce no diagnostics.
  </action>
  <verify>
    <automated>cargo check --quiet && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps 2>&1 | tee /dev/stderr | grep -E "^(warning|error):" | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Provider types are exported and tooling remains green with no unapproved runtime dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> CredentialsProvider | Provider may read from env/file/keyring; SDK receives only Credentials. |
| SDK -> logs/errors | Debug and errors must not expose credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Credentials Debug | high | mitigate | Full redaction implemented in Task 1; no prefix or length leaked. |
| T-01-02 | Information Disclosure | Error messages | medium | mitigate | CredentialsError only mentions missing cookie names, never values; from_provider maps parsing errors to Error::Config without echoing the header. |
| T-01-04 | Tampering | Provider trait object safety | low | mitigate | Trait requires Send + Sync; object-safe boxed future keeps Send bound. |
| T-01-SC | Tampering | Cargo installs | high | mitigate | No new runtime crates in this plan; if any appear, blocking human checkpoint verifies crates.io registry. |
</threat_model>

<verification>
- `cargo test --test auth_provider` passes.
- `cargo test --lib` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` produces no warnings.
</verification>

<success_criteria>
- `CredentialsProvider` trait and `CookieHeaderProvider` are public and tested.
- `GeminiClient::from_provider` constructs a client from any provider.
- `Debug` output for `Credentials` contains no secret substrings.
- No new runtime dependencies added without a blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/01-stabilize-v0-1-core/01-02-SUMMARY.md` when done.
</output>
