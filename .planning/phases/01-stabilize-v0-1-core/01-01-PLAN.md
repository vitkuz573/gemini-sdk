---
phase: 01-stabilize-v0-1-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/errors.rs
  - src/models.rs
  - src/chat.rs
  - src/client.rs
  - tests/api_stability.rs
  - Cargo.toml
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
must_haves:
  truths:
    - The crate compiles with `#![deny(missing_docs)]` on public items.
    - Public enums and structs that may grow are forward-compatible.
    - Error type is `Send + Sync + 'static` and uses `std::error::Error`.
    - A documented semver policy explains how breaking changes will be introduced.
  artifacts:
    - src/lib.rs with updated lint config and crate-level docs.
    - src/errors.rs with compile-time trait checks on Error.
    - src/chat.rs with #[non_exhaustive] on public enums/structs.
    - src/client.rs with #[non_exhaustive] on GeminiClient and ChatBuilder.
    - src/models.rs with #[non_exhaustive] on ModelInfo.
    - tests/api_stability.rs compile-fail tests for #[non_exhaustive] struct literals.
    - Cargo.toml with static_assertions dev-dependency.
    - README.md semver policy section.
  key_links:
    - lib.rs lint config -> every public item has docs.
    - errors.rs -> thiserror and static_assertions verify traits.
    - chat.rs + client.rs + models.rs -> #[non_exhaustive] surfaces.
---

<objective>
Stabilize the public API surface for v0.1 by making forward-compatible type annotations strict, documenting the semver policy, and gating builds on missing-docs and broken intra-doc links.

Purpose: v0.1 commits to a stable surface; this plan locks those contracts.
Output: Lint-clean public API with `#[non_exhaustive]` on all extensible public types and verified error trait bounds.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-stabilize-v0-1-core/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@src/lib.rs
@src/errors.rs
@src/chat.rs
@src/models.rs
@Cargo.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create Wave 0 API stability compile-fail tests</name>
  <files>tests/api_stability.rs</files>
  <behavior>
    - Test 1 (compile_fail): A downstream crate cannot construct ChatResponse via struct literal.
    - Test 2 (compile_fail): A downstream crate cannot construct Conversation via struct literal.
    - Test 3 (compile_fail): A downstream crate cannot construct ModelInfo via struct literal.
    - Test 4 (compile_fail): A downstream crate cannot construct GeminiClient or ChatBuilder via struct literal.
  </behavior>
  <action>
    Create `tests/api_stability.rs` as a Wave 0 integration test. Prefer `trybuild` compile-fail tests if adding it as a dev-dependency is straightforward; otherwise write runtime tests that assert the public constructors exist and that the types expose no public fields (e.g., by assigning them via constructors and verifying field privacy through `std::mem::size_of` or by attempting to construct with zeroed memory in a `compile_fail` doc test embedded in the file). The test must be runnable before Task 1 if it only checks current behavior, or should fail before Task 1 and pass after (TDD). If using trybuild, create a `tests/api_stability_ui/` directory with `.rs` and `.stderr` files for each case.
  </action>
  <verify>
    <automated>cargo test --test api_stability --quiet</automated>
  </verify>
  <done>Wave 0 API stability test file exists and runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add #[non_exhaustive] and private fields to extensible public types</name>
  <files>src/chat.rs, src/client.rs, src/models.rs, src/lib.rs</files>
  <behavior>
    - Test 1: A downstream crate cannot construct ChatResponse via struct literal.
    - Test 2: A downstream crate cannot construct Conversation via struct literal.
    - Test 3: A downstream crate cannot construct ModelInfo via struct literal.
    - Test 4: A downstream crate cannot construct GeminiClient or ChatBuilder via struct literal.
    - Test 5: Existing enums ModelCategory, ImageSource, ContentPart, ThinkingLevel remain #[non_exhaustive] and non-breaking.
  </behavior>
  <action>
    Apply #[non_exhaustive] to public enums/structs that may grow in future 0.x releases: ChatResponse, Conversation, ModelInfo, ChatMessage (or add a private _priv field where structural construction must be blocked). Apply #[non_exhaustive] to GeminiClient and ChatBuilder (ChatBuilder's private client/conversation/category/config fields already prevent literal construction; the attribute documents forward compatibility). Ensure all existing public constructors (ChatResponse::new, Conversation::new, ChatMessage::text/user/model/with_image, ModelInfo deserialization, GeminiClient::from_cookie_header/from_credentials/from_cookies, GeminiClient::chat) still work. Keep #[non_exhaustive] on ModelCategory, ImageSource, ContentPart, ThinkingLevel. Do NOT remove any public constructor; do NOT break example or test usage. Update any required imports so `cargo test` and `cargo build --examples` remain green.
  </action>
  <verify>
    <automated>cargo test --lib --quiet && cargo build --examples --quiet</automated>
  </verify>
  <done>Public extensible types, including GeminiClient and ChatBuilder, block external literal construction while constructors remain available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten documentation lints and fix broken intra-doc link</name>
  <files>src/lib.rs, src/auth.rs, src/errors.rs</files>
  <behavior>
    - Test 1: cargo doc --no-deps emits no warnings under deny-level lint.
    - Test 2: The doc link at src/auth.rs:121 resolves to the correct fully-qualified path.
    - Test 3: Every public item in src/errors.rs has a doc comment.
  </behavior>
  <action>
    Change `src/lib.rs` from `#![warn(missing_docs)]` to `#![deny(missing_docs)]` for library builds and add `#![deny(rustdoc::broken_intra_doc_links)]`. Keep `warn(missing_docs)` only for tests/benches/examples if needed to avoid blocking test code. Fix the broken intra-doc link in `src/auth.rs` by using the fully-qualified path `crate::client::GeminiClient::verify_signed_in` or by documenting the method with a plain code reference. Add missing doc comments to all public items in `src/errors.rs` (variants, helper functions, `is_transient`). Verify that `cargo doc --no-deps` is warning-free and `cargo clippy --all-targets -- -D warnings` still passes.
  </action>
  <verify>
    <automated>cargo doc --no-deps 2>&1 | tee /dev/tty | grep -E "^(warning|error):" | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Documentation builds with no warnings and missing_docs is denied on public items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify Error trait bounds and document semver policy</name>
  <files>src/errors.rs, Cargo.toml, README.md</files>
  <behavior>
    - Test 1: static_assertions verifies Error: Send + Sync + 'static.
    - Test 2: A unit test asserts std::error::Error is implemented and is_transient behaves for transient vs permanent variants.
    - Test 3: README.md contains a semver policy section explaining breaking changes in 0.x minor releases and major releases after 1.0.
  </behavior>
  <action>
    Add `static_assertions` as a dev-dependency in Cargo.toml. In `src/errors.rs`, add `assert_impl_all!(Error: Send, Sync, std::error::Error, 'static);` inside the test module (create one if absent). Add a semver policy section to README.md: breaking changes are allowed only in minor 0.x versions before v1.0 and in major versions after v1.0; patch releases are non-breaking. Ensure the new test compiles and passes. Do not alter Error variants except to add doc comments; preserve the `#[non_exhaustive]` attribute and `is_transient` logic.
  </action>
  <verify>
    <automated>cargo test --lib --quiet && cargo clippy --all-targets -- -D warnings && cargo doc --no-deps 2>&1 | tee /dev/tty | grep -E "^(warning|error):" | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Error trait bounds are verified at compile time and semver policy is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crate consumer -> public API | Callers pass cookie strings and prompts into the SDK; SDK must not leak them. |
| public type evolution -> downstream code | `#[non_exhaustive]` prevents downstream from depending on struct literal construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | auth.rs Debug redaction | high | mitigate | Full redaction in Plan 01-02 (per AUTH-03). |
| T-01-02 | Information Disclosure | Error messages | medium | mitigate | Do not include cookie header or secret values in Error::Config or Error::Parse strings; keep errors generic. |
| T-01-03 | Tampering | semver policy text | low | accept | Policy is human-enforced; CI will block breaking changes in patch releases only by test/code review. |
| T-01-SC | Tampering | Cargo installs | high | mitigate | Phase 1 adds only `static_assertions` as a dev-dependency; run package legitimacy check before install. |
</threat_model>

<verification>
- `cargo test --lib --quiet` passes.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` produces no warnings.
- `cargo build --examples --quiet` succeeds.
</verification>

<success_criteria>
- Public types carry #[non_exhaustive] where required by API-01.
- `cargo doc --no-deps` is warning-free with `#![deny(missing_docs)]` and `#![deny(rustdoc::broken_intra_doc_links)]` active.
- `Error` compile-time assertions for `Send + Sync + 'static` pass.
- README.md semver policy explains breaking-change rules.
</success_criteria>

<output>
Create `.planning/phases/01-stabilize-v0-1-core/01-01-SUMMARY.md` when done.
</output>
