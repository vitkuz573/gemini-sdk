---
phase: 01-stabilize-v0-1-core
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/integration_tests.rs
  - tests/proto_tests.rs
  - tests/fixtures/conversation_state.json
  - examples/multi_turn_chat.rs
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03
  - CHAT-05
  - MEDIA-01
must_haves:
  truths:
    - Text-only chat returns a complete ChatResponse with text via fixture parsing.
    - Multi-turn Conversation preserves state across turns in fixture-based tests.
    - Selecting a ModelCategory produces a request targeting that category's model.
    - Inline image upload path produces a usable attachment descriptor.
    - Examples compile for text chat, streaming, image upload, and multi-turn.
  artifacts:
    - tests/integration_tests.rs with multi-turn fixture test.
    - tests/proto_tests.rs with category-preservation and attachment tests (verify/extend).
    - examples/multi_turn_chat.rs demonstrating continue_conversation.
  key_links:
    - Conversation state fixture -> proto parser -> Conversation round-trip.
    - ChatBuilder::with_category -> build_inner_req_list slot 30 value.
    - ImageSource::from_bytes -> upload_attachments -> WebAttachment reference.
---

<objective>
Validate and document chat and media behavior for v0.1: text chat, streaming signature, multi-turn state, model category selection, and inline image upload.

Purpose: Prove that the stabilized API actually delivers the documented chat and media flows.
Output: Expanded fixture-based integration tests and a multi-turn example binary.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-stabilize-v0-1-core/01-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@tests/integration_tests.rs
@tests/proto_tests.rs
@examples/text_chat.rs
@examples/stream_chat.rs
@examples/image_chat.rs
@src/chat.rs
@src/client.rs
@src/proto/slots.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-turn conversation test using fixtures</name>
  <files>tests/integration_tests.rs, tests/fixtures/conversation_state.json</files>
  <behavior>
    - Test 1: A Conversation can be created, user/model turns added, and the history length grows.
    - Test 2: Conversation preserves the selected ModelCategory across clones.
    - Test 3: Client::continue_conversation accepts a Conversation and its category is used when building the request (mocked via slot inspection or by verifying ChatBuilder state).
  </behavior>
  <action>
    Extend tests/integration_tests.rs with a fixture-based multi-turn test. Use the existing `Conversation` API: create a conversation with `ModelCategory::Pro`, add a user turn and a model turn, clone it, and assert the cloned history and category are preserved. Add a test that calls `client.continue_conversation(conv.clone()).send_message("follow up")` with a mocked or stubbed client if available; if the codebase has no HTTP mocking yet, assert the builder carries the conversation reference and category without making a network call. Reuse `tests/fixtures/conversation_state.json` for parser coverage. Do not add live-network assertions; keep tests runnable without GEMINI_COOKIES.
  </action>
  <verify>
    <automated>cargo test --test integration_tests --quiet</automated>
    <automated>test -f tests/fixtures/conversation_state.json || (echo "Missing fixture: tests/fixtures/conversation_state.json" >&2 && exit 1)</automated>
  </verify>
  <done>Multi-turn conversation state is covered by fixture-based integration tests and the fixture file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify chat and media pipeline through proto tests</name>
  <files>tests/proto_tests.rs, src/chat.rs, src/proto/slots.rs</files>
  <behavior>
    - Test 1: build_inner_req_list slot 30 reflects the selected ModelCategory.
    - Test 2: A PreparedRequest with inline images yields the expected WebAttachment descriptors without uploading (unit test path).
    - Test 3: parse_chat_response returns a ChatResponse with text and optional thinking from existing fixtures.
  </behavior>
  <action>
    In tests/proto_tests.rs, add or strengthen tests for model category preservation: build requests with `ModelCategory::Fast`, `Thinking`, `Pro`, and assert slot 30 equals the expected enum value. For MEDIA-01, verify that `ImageSource::from_bytes` encodes data correctly and that `prepare_request` extracts inline images into `PreparedRequest.inline_images`. Reuse existing fixture tests for `parse_chat_response`. Do not change the 97-slot layout; only add assertions. Ensure tests pass without network access.
  </action>
  <verify>
    <automated>cargo test --test proto_tests --quiet</automated>
  </verify>
  <done>Model category and inline image paths are verified through fixture/proto tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add multi-turn example and verify all examples compile</name>
  <files>examples/multi_turn_chat.rs, Cargo.toml</files>
  <action>
    Create examples/multi_turn_chat.rs that reads GEMINI_COOKIES, constructs a GeminiClient, sends an initial message with `client.chat()`, then continues the conversation with `client.continue_conversation(conversation).send_message(...)`, printing both responses. Follow the style of examples/text_chat.rs and examples/image_chat.rs (tracing_subscriber init, env var error, no unwrap on SDK results). Register the example in Cargo.toml with an `[[example]]` block. Run `cargo build --examples` to confirm text_chat, image_chat, stream_chat, and multi_turn_chat all compile.
  </action>
  <verify>
    <automated>cargo build --examples --quiet</automated>
  </verify>
  <done>A multi-turn example exists and all examples compile cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files -> parser | Static fixtures must not contain real credentials or PII. |
| test code -> examples | Examples read GEMINI_COOKIES at runtime; tests must not require them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | Fixture files | low | mitigate | Audit fixtures for cookie values or personal data before committing new ones; use synthetic IDs like `c_abc`. |
| T-01-06 | Denial of Service | Parser panic on malformed fixture | medium | mitigate | Use Result-returning parse functions and defensive indexing; tests assert graceful errors. |
| T-01-07 | Spoofing | Model category validation | low | accept | Category is caller-supplied; validation is implicit via enum (no untrusted input path). |
| T-01-SC | Tampering | Cargo installs | high | mitigate | No new dependencies in this plan. |
</threat_model>

<verification>
- `cargo test --test integration_tests --quiet` passes.
- `cargo test --test proto_tests --quiet` passes.
- `cargo build --examples --quiet` succeeds.
- `cargo clippy --all-targets -- -D warnings` passes.
</verification>

<success_criteria>
- Multi-turn conversation test exists and passes without live cookies.
- Model category is asserted in slot-building tests.
- Inline image upload path is covered by unit/proto tests.
- All examples (text, image, stream, multi-turn) compile.
</success_criteria>

<output>
Create `.planning/phases/01-stabilize-v0-1-core/01-03-SUMMARY.md` when done.
</output>
