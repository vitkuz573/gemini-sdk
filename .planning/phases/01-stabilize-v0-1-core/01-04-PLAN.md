---
phase: 01-stabilize-v0-1-core
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/retry.rs
  - src/errors.rs
  - src/lib.rs
  - Cargo.toml
  - README.md
autonomous: true
requirements:
  - REL-01
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
must_haves:
  truths:
    - Retries use exponential backoff with jitter for transient HTTP errors and rate limits.
    - `cargo test --all-targets` passes without live cookies.
    - `cargo clippy --all-targets -- -D warnings` passes.
    - `cargo doc --no-deps` builds with no warnings.
    - Crate manifest and metadata are publishable (dry-run succeeds).
  artifacts:
    - src/retry.rs with verified backoff parameters and transient classification.
    - src/errors.rs with no clippy/doc warnings.
    - Cargo.toml with readme field, reviewed metadata, and publish exclude list.
    - README.md with usage, feature, and semver sections.
  key_links:
    - retry::with_backoff -> Error::is_transient -> exponential backoff config.
    - Cargo.toml metadata -> cargo publish --dry-run.
---

<objective>
Lock reliability behavior and tooling gates for v0.1: verify retry/backoff, ensure full test suite, clippy, docs, and examples pass, and prepare the crate for publication.

Purpose: v0.1 must ship with a green CI gate and publishable manifest.
Output: All tooling commands pass and cargo publish --dry-run succeeds.
</objective>

<execution_context>
@/home/vitaly/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/vitaly/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-stabilize-v0-1-core/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/retry.rs
@src/errors.rs
@src/lib.rs
@Cargo.toml
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify public transient classification and retry behavior</name>
  <files>src/retry.rs, src/errors.rs</files>
  <behavior>
    - Test 1: Error::is_transient returns true for 429, 5xx, Error::Transient, Error::RateLimited, and Error::Timeout; false for 400/404 and Error::BadRequest.
    - Test 2: with_backoff retries a transient operation at least once before failing.
    - Test 3: with_backoff does not retry a permanent 4xx (non-429) operation.
  </behavior>
  <action>
    Ensure `src/retry.rs` uses `backoff::ExponentialBackoff` with initial_interval 500ms, max_interval 8s, and max_elapsed_time 30s (document these values). Add or extend unit tests in `src/retry.rs` that exercise the public Error::is_transient API first, then instrument the internal `with_backoff` helper via a stub operation returning `Result<(), Error>`. Since constructing reqwest errors directly is difficult, build Error::Api variants from `reqwest::StatusCode` where possible, and test the retry count by returning a transient error N times then success. Keep the retry helper non-public; tests live in the same file.
  </action>
  <verify>
    <automated>cargo test --lib retry --quiet</automated>
  </verify>
  <done>Public transient classification and internal retry/backoff behavior are verified.</done>
</task>

<task type="auto">
  <name>Task 2: Fix clippy warnings across all source files</name>
  <files>src/lib.rs, src/**/*.rs</files>
  <action>
    Run `cargo clippy --all-targets -- -D warnings` and fix any new warnings introduced by the prior three plans. Pay special attention to: missing docs on newly public items, redundant closures, unused imports in new test files, and any `Debug` or `Display` lints. If clippy requires code changes that affect public API, document them in the plan summary. Do not suppress warnings unless the suppression matches an existing pattern in src/lib.rs. After fixes, re-run the full clippy command.
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <done>Clippy passes with zero warnings on all targets.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize docs, examples, and publishability</name>
  <files>Cargo.toml, README.md, src/lib.rs</files>
  <action>
    Add `readme = "README.md"` to Cargo.toml if not present and review required crate metadata (name, version, authors, edition, license, description, repository, keywords, categories, rust-version). Add an `exclude` array to Cargo.toml that prevents `.planning/`, `.opencode/`, `docs/`, `benches/`, `tests/`, `examples/`, `clippy.toml`, `rustfmt.toml`, `AGENTS.md`, and `CONTRIBUTING.md` from being packaged. Ensure README.md contains: quick start matching src/lib.rs doc example, feature flag descriptions (`browser-attestation`), semver policy, and a note that live-cookie tests require GEMINI_COOKIES. Run `cargo doc --no-deps` and fix any remaining diagnostics including broken intra-doc links. Run `cargo test --all-targets` without GEMINI_COOKIES and confirm it passes (live tests should remain ignored). Finally run `cargo publish --dry-run` and fix any packaging errors. Do not actually publish; the dry-run is the gate.
  </action>
  <verify>
    <automated>cargo test --all-targets --quiet && cargo doc --no-deps 2>&1 | tee /dev/stderr | grep -E "^(warning|error):" | wc -l | xargs -I{} test {} -eq 0 && cargo publish --dry-run --allow-dirty 2>&1 | tee /dev/stderr | grep -c "error:" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Full test suite, docs, and publish dry-run pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> upstream Gemini | Retry logic decides whether to replay requests; must avoid infinite retry loops. |
| crate -> crates.io | Publish manifest must not include secrets, build artifacts, or .planning files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Denial of Service | retry.rs | high | mitigate | max_elapsed_time caps total backoff; permanent errors are not retried. |
| T-01-09 | Information Disclosure | Cargo.toml / README | medium | mitigate | Review README and metadata for embedded secrets or personal info before dry-run. |
| T-01-10 | Tampering | Cargo.lock / publish | low | accept | Use `cargo publish --dry-run` to validate manifest; final publish is Phase 6. |
| T-01-SC | Tampering | Cargo installs | high | mitigate | No new dependencies in this plan; package legitimacy already audited in 01-RESEARCH.md. |
</threat_model>

<verification>
- `cargo test --all-targets --quiet` passes without live cookies.
- `cargo clippy --all-targets -- -D warnings` passes.
- `cargo doc --no-deps` produces no warnings.
- `cargo publish --dry-run` succeeds.
</verification>

<success_criteria>
- REL-01 retry behavior is covered by unit tests.
- `cargo test --all-targets` is green.
- `cargo clippy --all-targets -- -D warnings` is green.
- `cargo doc --no-deps` is warning-free.
- `cargo publish --dry-run` succeeds.
- README.md reflects v0.1 usage, features, and semver policy.
</success_criteria>

<output>
Create `.planning/phases/01-stabilize-v0-1-core/01-04-SUMMARY.md` when done.
</output>
